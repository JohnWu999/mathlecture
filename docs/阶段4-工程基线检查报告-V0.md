# 阶段4｜工程基线检查报告 V0

## 检查时间

2026-06-01

## 检查对象

- 仓库：`/home/ubuntu/Workspace/math-young-lecturer`
- 技术栈：Next.js 14 App Router / React 18 / TypeScript / Prisma / NextAuth / Tailwind CSS
- 当前推进点：三类分享海报 V1 已认可，进入代码实施前批次0工程基线检查。

## 构建结果

命令：

```bash
npm run build
```

结果：✅ PASS

关键输出：

- Next.js 14.2.35
- Production build 编译成功
- Static pages 生成成功：14/14
- 当前路由可构建，包括：首页、你问我答、提交问题、问题详情、项目营、项目详情、小组协作、成果广场、个人中心、老师工作台、注册/登录、API 路由等。

## 当前可用页面/路由基线

### 前台页面

- `/` 首页
- `/qa` 你问我答
- `/qa/ask` 提交问题
- `/qa/question/[id]` 问题详情
- `/projects` 项目营
- `/projects/[id]` 项目详情
- `/groups/[id]` 项目小组协作空间
- `/hall` 成果广场
- `/profile` 个人中心
- `/login` 登录
- `/register` 注册

### 后台/工作台

- `/teacher` 老师工作台

### API 基线

- `/api/questions/*`
- `/api/answers/*`
- `/api/projects/*`
- `/api/groups/*`
- `/api/hall`
- `/api/teacher/*`
- `/api/user/profile`
- `/api/auth/[...nextauth]`

## 重要发现

1. **工程基线稳定**
   - 当前代码可以生产构建，说明可以进入功能升级，不需要先做大规模救火。

2. **Next build 当前跳过 TypeScript 类型校验**
   - 构建输出出现 `Skipping validation of types`。
   - 后续进入实装前，建议补一个单独类型检查命令，避免 Prisma/NextAuth/接口字段升级时类型错误被漏掉。

3. **当前页面仍保留旧产品表达的可能性较高**
   - 已在代码中看到首页存在“项目官”“24h 内回复”“被采纳赚积分”等旧文案，应在批次1/3中统一替换为最终规则：探索家、72小时认领、不公开积分、不做排名。

4. **下一步不需要继续做静态稿**
   - 首页 V1 与海报 V1 已形成足够视觉依据；当前应转入工程落地。

## 下一步建议

进入《代码实施前工程实施计划 V0》的：

### 批次 1｜产品常量与视觉 token 抽离

优先新增：

- `lib/product-copy.ts`
- `lib/visual-tokens.ts`
- `components/brand/infinity-logo.tsx`
- `components/brand/growth-motifs.tsx`
- `components/ui/badge.tsx`
- `components/ui/card.tsx`

然后再进入：

### 批次 2｜数据库模型升级

重点补齐：

- 分享授权状态
- 审核状态
- 三类身份进度
- 能力标签记录
- 分享资产
- 管理员导出日志

## 当前阶段结论

✅ 可以进入代码实施。  
建议先做小步提交：先抽离产品常量和视觉 token，再改首页与数据模型，避免一次性大改导致回归风险。
