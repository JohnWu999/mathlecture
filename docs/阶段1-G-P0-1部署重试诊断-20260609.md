# 阶段1-G P0-1｜部署重试诊断与修正记录

时间：2026-06-09 16:58 CST  
公网地址：`http://159.75.144.28/math-young-lecturer`  
本地分支：`batch9/review-authorization`  
部署提交：`cec0d48 docs: diagnose stage1g server ssh access`

## 1. 修正结论

经犟爸提醒“以前一直能正常访问”，已回查历史部署方式。

前两次失败不是服务器不可达，也不是生产机 SSH 服务异常；直接原因是本轮排查时误用了默认 `.ssh/` key 和非本项目授权 key，遗漏了项目早期交接记录中的专用部署 key。

本文不记录任何真实私钥路径、公钥原文、密码、token、连接串或环境变量值。

## 2. 恢复访问验证

使用项目专用部署 key 后，生产服务器登录恢复：

```text
connected:VM-0-6-ubuntu:ubuntu
```

部署用户：

```text
ubuntu@159.75.144.28
```

## 3. 生产部署结果

已完成阶段1-G P0-1 生产部署：

- 保留生产 `.env`；
- 不使用危险的 `rsync --delete .next/standalone/` 覆盖生产根目录；
- 按 worktree standalone 结构同步构建产物；
- reload PM2 `math-young-lecturer`。

线上 `DEPLOY_BUILD.json` 标记：

```json
{
  "commit": "cec0d48",
  "branch": "batch9/review-authorization",
  "stage": "stage1g-p01-question-login-gate",
  "note": "No secrets included. P0-1 ask page login gate deployment."
}
```

## 4. 公网与 PM2 验证

公网 `/math-young-lecturer/qa/ask`：

```text
GET /math-young-lecturer/qa/ask: 200
登录后提问=True
反馈准确推送给你=True
公开展示时隐藏孩子姓名=True
匿名提问（保护提问安全感）=False
```

匿名写入保护：

```text
POST /math-young-lecturer/api/questions: 401
{"error":"请先登录"}
```

PM2：

```text
status: online
script path: /var/www/math-young-lecturer/server.js
exec cwd: /var/www/math-young-lecturer
unstable restarts: 0
```

## 5. 后续注意

后续部署不要只尝试默认 `.ssh/` key；应先回查项目交接记录中的专用部署 key。文档和对外汇报只写“项目专用部署 key”，不写真实路径或密钥内容。
