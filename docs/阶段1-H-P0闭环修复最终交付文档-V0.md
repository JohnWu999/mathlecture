# 阶段1-H｜P0闭环修复最终交付文档 V0

时间：2026-06-15 19:48 CST  
分支：`batch9/review-authorization`  
状态：P0 闭环修复已完成并已部署生产；本地规则测试与生产构建通过，公网 smoke/API/PM2 已完成验证。

---

## 一、修复目标

本轮进入 P0 闭环修复，重点处理 Student 朵朵、Teacher 知知、管理员 Apple 试跑反馈中会直接阻断真实使用信任的闭环问题：

1. 上传入口不能是假入口：头像、老师/咨询二维码必须能上传、回显、保存。
2. 老师审核不能“点完没反应”：通过后要有即时反馈，并能在已通过归档中找到。
3. 项目协作空间不能只显示 0 位成员：需要展示成员名单、开始时间、截止时间和任务卡更新规则。
4. 数据看板不能只有数字：老师/管理员点击数字或模块后必须看到具体明细。
5. OCR 未真实接入时不做假承诺：继续保留“上传照片 + 手动补充文字”的诚实降级说明。

---

## 二、已完成的 P0 闭环修复

### 1. 学生头像上传闭环

已完成：

- `/api/uploads` 新增 `avatar` 上传类型；
- 上传类型规则补充头像图片限制：JPEG / PNG / WEBP，5MB；
- 头像上传限定学生账号使用；
- `/api/user/profile` 新增 `PATCH`，可保存头像 URL 到 `User.avatar`；
- 个人中心头像区改为真实文件选择器；
- 上传成功后即时预览并显示“头像已更新”。

验收点：个人中心“上传头像”不再是假入口。

### 2. 老师/咨询二维码上传闭环

已确认并纳入本轮规则：

- 管理员后台“企业微信/咨询入口配置”使用真实 `type="file"` 上传；
- 上传走 `/api/uploads` 的 `consultation-qr`；
- `consultation-qr` 限定管理员角色；
- 上传成功后回显二维码预览；
- 保存后写入咨询入口配置。

验收点：管理员 Apple 可以上传老师二维码图片，并在后台保存咨询入口配置。

### 3. 老师审核归档与状态反馈

已完成：

- 老师工作台“审核问题”新增“待审核问题 / 已通过问题”切换；
- 点通过后，题目即时从待审核移入已通过问题归档；
- 已通过问题卡片显示“已通过”标识；
- 问题列表 API 返回题目图片与手动补充/识别文字，方便老师复核题型；
- 老师工作台“审核讲题”新增“待审核讲题 / 已通过讲题”切换；
- 讲题视频点通过后即时移入已通过讲题归档；
- 成果审核继续保留待审核 / 已公开 / 已撤回授权视图。

验收点：Teacher 知知点“通过”后，能立刻知道是否生效，也能回头查看已通过了哪些题和讲题视频。

### 4. 项目协作空间运营信息补齐

已完成：

- 小组协作 API 返回成员详情，不再只返回成员数量；
- 协作空间展示“成员名单”；
- 协作空间展示开始时间、截止时间；
- 截止时间按小组创建时间 + 项目 `durationDays` 推导；
- 任务卡区域补充说明：“每天根据项目开始时间和当前 Day 更新任务卡；老师可按小组进度调整 Day，截止时间用于提醒收尾，不做排名。”

验收点：Student 朵朵进入协作空间后，能看到自己所在小组成员、项目节奏和任务卡按天更新逻辑。

### 5. 老师数据看板钻取

已完成：

- 老师数据看板 API 返回最近问题、最近讲题、待审核事项明细；
- 老师看板卡片改为可点击按钮；
- 点击后出现“数据明细”面板；
- 总用户、总问题、总讲题、待处理、项目数、报名人数均提供明细或说明。

验收点：Teacher 知知点击看板数据后，能看到对应具体数据。

### 6. 管理员后台总览与数据看板钻取

已完成：

- 管理员顶部统计卡片改为可点击；
- 点击后出现“数据明细”面板；
- 学习者、老师/管理员、项目、问题、视频、作品、报名意向均提供具体条目；
- 后台模块总览卡片改为可点击；
- 点击模块后出现“模块明细”面板。

验收点：管理员 Apple 点击后台模块或数据卡片后，不再停留在“点不动”的静态框。

---

## 三、继续保留的边界

### OCR 自动识别

本轮没有伪造 OCR 自动识别能力。当前策略仍是：

- 可以上传题目照片；
- 需要手动补充题目文字；
- 页面明确说明 OCR 自动识别是下一批接入能力。

原因：没有真实 OCR 服务时，不能把“上传照片”包装成“自动识别”。后续若要进入 OCR P0，需要单独接入识别服务、识别中状态、失败兜底和人工修改入口。

---

## 四、新增防回归测试

新增：`tests/stage1h-p0-closure-rules.test.mjs`

覆盖 4 类 P0 闭环：

1. 学生头像上传是真闭环：上传类型、权限、文件选择器、保存 API、状态反馈；
2. 老师审核归档：已通过问题、已通过讲题、点击通过后即时移入归档；
3. 项目协作空间：成员详情、开始时间、截止时间、任务卡更新规则；
4. 老师/管理员数据看板：可点击钻取、明细面板、后台模块明细。

---

## 五、本轮主要代码变更文件

- `app/profile/page.tsx`
- `app/api/user/profile/route.ts`
- `app/api/uploads/route.ts`
- `lib/upload-rules.mjs`
- `app/teacher/page.tsx`
- `app/api/questions/route.ts`
- `app/api/teacher/answers/route.ts`
- `app/api/teacher/dashboard/route.ts`
- `app/groups/[id]/page.tsx`
- `app/api/groups/[id]/messages/route.ts`
- `app/admin/page.tsx`
- `tests/stage1h-p0-closure-rules.test.mjs`
- `docs/阶段1-H-三身份真实试跑反馈处理单-V0.md`
- `docs/项目流程控制台.md`

---

## 六、验证记录

```bash
node --test tests/stage1h-p0-closure-rules.test.mjs
# 4/4 pass

node --test tests/stage1h-p0-closure-rules.test.mjs tests/stage1h-trial-feedback-rules.test.mjs tests/growth-passport-ui-rules.test.mjs tests/project-camp-ui-rules.test.mjs tests/stage1g-p03-project-registration-login-rules.test.mjs tests/stage1g-p02-qa-interaction-layer-rules.test.mjs
# 23/23 pass

npm run build
# exit 0，Next.js production build compiled successfully
```

---

## 七、生产部署记录（2026-06-16 11:00 CST）

### 1. 部署版本

- 部署分支：`batch9/review-authorization`
- 部署 commit：`a648f51 stage1h: close role trial p0 loops`
- 线上应用目录：`/var/www/math-young-lecturer`
- PM2 应用：`math-young-lecturer`
- 生产入口：`http://159.75.144.28/math-young-lecturer`

### 2. 部署包与备份

- 本地归一化部署包：`/tmp/math-young-lecturer-a648f51-stage1h-normalized.tar.gz`
- 部署包 SHA256：`6febca5335b7ebb354fd56fa165410e14db80fdf47a069bd08963260d5755b4d`
- 生产备份包：`/var/backups/math-young-lecturer/math-young-lecturer-a648f51-pre-stage1h-20260616T025909Z.tar.gz`

本次部署按 worktree standalone 归一化方式执行，已保留生产 `.env`、上传目录和现有运行时边界；发布包根目录包含 `server.js`、`node_modules/next`、`.next/static`、`public`、`prisma` 与 `DEPLOY_BUILD.json`。

### 3. 本地验证

```bash
node --test tests/stage1h-p0-closure-rules.test.mjs
# 4/4 pass

node --test tests/stage1h-p0-closure-rules.test.mjs tests/stage1h-trial-feedback-rules.test.mjs tests/growth-passport-ui-rules.test.mjs tests/project-camp-ui-rules.test.mjs tests/stage1g-p03-project-registration-login-rules.test.mjs tests/stage1g-p02-qa-interaction-layer-rules.test.mjs
# 23/23 pass

npm run build
# exit 0，Next.js production build compiled successfully
```

### 4. 生产 smoke / API / PM2 验证

公网页面与接口：

- `/math-young-lecturer`：200
- `/math-young-lecturer/login`：200
- `/math-young-lecturer/profile`：307 → `/math-young-lecturer/login`
- `/math-young-lecturer/teacher`：307 → `/math-young-lecturer/login`
- `/math-young-lecturer/admin`：307 → `/math-young-lecturer/login`
- `/math-young-lecturer/qa`：200
- `/math-young-lecturer/qa/ask`：200
- `/math-young-lecturer/projects`：200
- `/math-young-lecturer/hall`：200
- `/math-young-lecturer/api/projects`：200
- `/math-young-lecturer/api/hall`：200
- `/math-young-lecturer/api/questions?status=OPEN`：200
- `/math-young-lecturer/api/teacher/dashboard` 未登录：401
- `POST /math-young-lecturer/api/uploads` 未登录：401
- `POST /math-young-lecturer/api/questions` 未登录：401
- `POST /math-young-lecturer/api/projects/demo/register` 未登录：401

生产构建中已检索到本轮关键标记：`头像已更新`、`已通过问题`、`已通过讲题`、`成员名单`、`开始时间`、`截止时间`、`每天根据项目开始时间和当前 Day 更新任务卡`、`数据明细`、`模块明细`、`OCR 自动识别会作为下一批接入`。

PM2 验证：

- `math-young-lecturer`：online
- `unstable_restarts`：0
- 进程脚本：`/var/www/math-young-lecturer/server.js`
- error log 文件最后更新时间为 2026-06-09，部署后未出现新的错误日志写入。

### 5. 当前状态与下一步建议

当前状态：

- Stage1-H P0 闭环修复已部署生产；
- 本地规则测试 23/23 通过；
- `npm run build` 通过；
- 公网 smoke/API/PM2 验证通过；
- 代码已提交并推送到 GitHub：`a648f51`。

建议下一步：

1. 用朵朵 / 知知 / Apple 三个真实账号做人工验收：头像上传、老师审核归档、协作空间成员与日期、老师/管理员数据钻取、二维码上传；
2. 若公网上传失败，优先检查生产 `UPLOAD_DIR`、静态文件访问路径和目录权限；
3. OCR 是否进入下一批，单独定决策：真实接入或继续明确降级。

---

## 八、第二轮真实试跑反馈修复记录（2026-06-16 11:51 CST）

在上一轮 P0 已生产部署后，朵朵 / 知知 / Apple / 新学员注册继续试跑暴露出第二层闭环问题。本轮已完成代码修复、本地验证、生产部署与公网 smoke/API/PM2 验证。

### 1. 本轮已修复

- 上传文件可见性：新增 `app/uploads/[kind]/[filename]/route.ts`，头像与咨询二维码上传后可通过运行时路由读回，避免生产目录中文件无法被浏览器显示。
- 项目时间与 Day：协作空间开始时间优先取学习者加入小组、项目开通、报名时间；当前 Day 按学习者真实开始日期与今天计算，不再用旧小组创建时间冒充。
- 看板点击反馈：老师 / 管理员看板点击后滚动并聚焦到明细面板，增加 `aria-expanded`、`aria-controls` 和“已展开”提示。
- 审核素材可打开：老师端讲题视频、成果作品链接统一规范化为部署路径下可打开链接，并提供复制兜底说明。
- 新学员注册：注册页年级改为“大班、一年级、二年级、三年级”；地域改为省份 + 城市；注册 API 默认开放基础参与。
- 免费项目报名：免费项目报名写入确认状态，文案明确“满人数即可成组，老师只查看学习状态”。

### 2. 本轮未伪装为已完成的后续能力

- 每日任务验收 / 解锁模式：需定义 Day 任务标准、提交证据、老师/系统确认和解锁规则，列入 P1。
- 最终成果展示模板：建议支持照片、视频、作品链接三类，但需和审核授权流程一起设计，列入 P1。
- 报名意向库完整迁移到老师工作台：涉及老师/管理员权限边界，列入 P1 信息架构调整。
- 各大数据库导出：需做权限校验、导出范围和审计日志，列入 P1 数据运营能力。

### 3. 本地验证

```bash
node --test tests/stage1h-second-trial-feedback-rules.test.mjs
# 6/6 pass

node --test tests/stage1h-second-trial-feedback-rules.test.mjs tests/stage1h-p0-closure-rules.test.mjs tests/stage1h-trial-feedback-rules.test.mjs tests/growth-passport-ui-rules.test.mjs tests/project-camp-ui-rules.test.mjs tests/stage1g-p03-project-registration-login-rules.test.mjs tests/stage1g-p02-qa-interaction-layer-rules.test.mjs
# 29/29 pass

npm run build
# exit 0，Next.js production build compiled successfully
```

处理单：`docs/阶段1-H-第二轮真实试跑反馈处理单-V0.md`

### 4. 第二轮生产部署记录（2026-06-16 12:59 CST）

- 部署分支：`batch9/review-authorization`
- 部署 commit：`ebc1599 stage1h: address second trial feedback`
- 部署包：`/tmp/math-young-lecturer-ebc1599-stage1h-second-trial-feedback.tar.gz`
- 部署包 SHA256：`a0bce33b52b92b7a4971c80fdb5de8c31f04f9086209e21654f93f9dba529b8e`
- 线上应用目录：`/var/www/math-young-lecturer`
- PM2 应用：`math-young-lecturer`
- 生产入口：`http://159.75.144.28/math-young-lecturer`

部署说明：本次继续按 worktree standalone 结构发布，保留生产运行目录、PM2 应用、上传目录与现有运行边界；发布后补齐 standalone 根 `node_modules/next`，并将生产上传目录对齐到 nginx 共享目录 `/var/www/math-young-lecturer-shared/uploads`，确保 `/math-young-lecturer/uploads/...` 图片可公网读回。

公网 smoke / API 验证：

- `/math-young-lecturer`：200
- `/math-young-lecturer/login`：200
- `/math-young-lecturer/profile`：200（未登录展示登录/成长护照引导壳，no-store）
- `/math-young-lecturer/teacher`：200（未登录展示登录/成长护照引导壳，no-store）
- `/math-young-lecturer/admin`：200（未登录展示登录/成长护照引导壳，no-store）
- `/math-young-lecturer/qa`：200
- `/math-young-lecturer/qa/ask`：200
- `/math-young-lecturer/projects`：200
- `/math-young-lecturer/hall`：200
- `/math-young-lecturer/api/projects`：200
- `/math-young-lecturer/api/hall`：200
- `/math-young-lecturer/api/questions?status=OPEN`：200
- `/math-young-lecturer/uploads/avatar/smoke.gif`：200，`Content-Type=image/gif`
- `POST /math-young-lecturer/api/uploads` 未登录：401
- `POST /math-young-lecturer/api/questions` 未登录：401
- `POST /math-young-lecturer/api/projects/proj_demo_001/register` 未登录：401
- `POST /math-young-lecturer/api/teacher/dashboard` 未登录：401

生产版本验证：`DEPLOY_BUILD.json` 已更新为 `stage1h-second-trial-feedback` / `ebc1599`。

PM2 验证：

- `math-young-lecturer`：online
- 进程脚本：`/var/www/math-young-lecturer/server.js`
- cwd：`/var/www/math-young-lecturer`
- `UPLOAD_DIR`：`/var/www/math-young-lecturer-shared/uploads`
- 说明：部署过程中曾因 standalone 根 `node_modules/next` 未同步出现一次短暂启动错误，已补同步并恢复；当前服务在线，公网验证通过。
