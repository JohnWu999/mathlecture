# 【数学小讲师联盟】项目流程控制台

> 依据：《数学小讲师联盟·设计总纲》V0  
> 执行规则：《【数学小讲师联盟】·执行流程V0》  
> 当前目标：一周内重建可运行 MVP

---

## 当前阶段

阶段1-F｜入口真实性与业务闭环推进：全站入口清单可视化 / 未完成入口降级 / 试运营空状态与后台可追踪状态审计

## 是否允许进入下一阶段

是。

原因：阶段1最终交付文档 V1 已完成；阶段2最终交付文档 V2 已完成，已按最新结论整合页面结构、12个新增产品问题、温暖提示语、公开数据、分享长图、分享范围选择、项目解锁/组队、免费/付费权益、付费报名、教师权限、项目后反馈问卷、首屏定位、审核通知、题目图片识别/热度、项目有效期、免费项目质量边界、证书能力展示、请假退出、成果点赞、身份点亮、邀请影响力统计等规则，并排除已被替换、降级或推翻的内容；阶段3页面原型与 MVP 功能拆解 V2 已生成，已吸收阶段3 V1反馈中的低年级提问表单、72小时认领、分享范围、项目反馈频率、我的影响力、响应式导航、能力星云、教育性文案、热度说明、付费项目包、退费规则、统一封面、付费/退费记录等规则。UI 低保真线框图 V0 已生成，覆盖首页、你问我答、提交问题、热度弹窗、小讲师认领、项目营、项目协作空间、成果广场、个人中心、老师工作台、管理员后台。当前可继续推进 MVP 开发任务清单 V0。

---

## 阶段1交付物状态

- ✅ 首页内容清单：已形成 V1，已吸收犟爸关于首页呈现、成果广场、免费提问、自主认领、系统锁定、发布前审核的最终讨论意见
- ✅ 分页内容清单：已根据犟爸反馈形成 V2，主导航收敛为：首页、你问我答、项目营、成果广场、个人中心；登录/注册改为弹窗或独立路由能力，不作为主导航；明确未登录/已登录可见边界；问答闭环与 PBL 均列为 P0
- ✅ 删除项说明：已根据犟爸确认反馈形成 V7，整合登录注册主导航删除、个人中心按身份分流、老师工作台入口、成果授权、项目解锁、公开徽章但不公开积分等规则；并进一步修正为“学习者账号/老师账号/管理员账号”三类账号，明确学习者账号就是孩子账号，家长只辅助操作；前台主身份优化为“提问者 / 小讲师 / 探索家”，采用并列点亮 + 1–3 星成长；主身份在头像旁轻展示、点击头像弹出孩子身份卡片，能力徽章不公开展示，放入成长护照，采用老师标签累计后系统自动点亮；提问和小讲师视频不作为主要付费业务，P0 付费核心建议放在进阶项目包/主题项目营；免费额度按每个孩子计算，免费基础项目采用异步轻协作，退出记录用于控制后续体验项目机会
- ✅ 阶段1最终交付文档：已形成 V1，整合首页内容清单 V1、分页内容清单 V2、删除项说明 V7，可作为阶段2页面结构草图依据

---

## 当前正在处理

阶段3页面原型与 MVP 功能拆解 V2 已完成；UI 低保真线框图 V2 已完成；MVP 开发任务清单 V1 已完成；已完成全站数据动线自检；已补充数据资产工作台归属、管理员导出/下载权限、知识产权与反扒机制、AI生成项目入库与产出标准；已完成全站 UI 视觉风格无限生长版、首页高保真 V1。三类分享海报已从 V0 根据反馈升级到 V1，并获得犟爸认可；已生成《代码实施前工程实施计划 V0》与 agentic workers 实施计划。批次0工程基线检查已完成，`npm run build` 通过。为避免代码实施中版本混乱和回退丢功能，已新增《代码实施管控与回退预案 V0》。已归档《积分规则展示与成长能量机制 V0》，确认积分规则主解释放在个人中心/成长护照，行动页轻提示，首页不强调积分，家长视角补充反排名说明。代码实施前准备已自检并补齐；批次1产品常量与视觉 token 抽离已完成收尾。批次2成长能量 / 积分账本数据模型与接口准备已完成：已新增成长能量规则测试与 `lib/growth-energy-rules.mjs`，扩展 `prisma/schema.prisma` 中私密成长能量流水、身份星级进度、能力标签日志、热度记录、分享资产、导出日志等基础模型，并在讲解审核/采纳与个人中心接口预留成长能量写入和展示能力。批次3你问我答 P0 数据字段与流程接口已完成：已按 TDD 新增 QA 流程规则测试并接入提问字段、热度唯一规则、72小时认领、问题审核与提问成长能量接口。批次4你问我答前端页面接入 / UI 流程落地已完成：已新增 Q&A UI 规则测试与 `lib/qa-ui-rules.mjs`，并接入提问页、问题列表页、问题详情页、热度按钮、认领/72小时提示、讲解提交分享范围与老师问题审核 UI。批次5个人中心 / 成长护照 / 我的数学成长能量 UI 落地已完成：已新增成长护照 UI 规则测试与 `lib/growth-passport-ui-rules.mjs`，并接入个人中心的成长能量、来源用途说明、最近流水、三身份星级、能力徽章与反排名家长说明。批次6项目营 / 项目卡准备度与参与条件 UI 落地已完成：已新增项目营 UI 规则测试与 `lib/project-camp-ui-rules.mjs`，并接入项目列表页、项目详情页的准备度/参与条件、组队状态、有效期、项目类型、报名意向和探索成长提示。批次7项目协作空间 / 任务卡 / 作品提交 UI 落地已完成：已新增项目协作 UI 规则测试与 `lib/project-collab-ui-rules.mjs`，并接入小组协作空间的任务卡、留言流、过程记录、作品提交、项目反馈、探索成长提示和 AI 使用边界。当前已进入批次8成果广场 / 分享授权 / 统一作品展示 UI 落地：已新增成果广场 UI 规则测试与 `lib/outcome-hall-ui-rules.mjs`，并接入成果广场列表、成果详情、分享授权说明、统一成果卡和审核+授权公开边界。

---

## 阶段1验收标准

1. 每个信息项能用一句话说清“用户看到它能理解什么”。
2. 没有两个信息项表达同一意思。
3. 总信息项不超过 7 个/页，超出需说明为什么必须保留。

---

## 交付物路径

- 首页内容清单 V0：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段1-首页内容清单-V0.md`
- 首页内容清单 V1：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段1-首页内容清单-V1.md`
- 分页内容清单 V0：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段1-分页内容清单-V0.md`
- 分页内容清单 V1：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段1-分页内容清单-V1.md`
- 分页内容清单 V2：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段1-分页内容清单-V2.md`
- 删除项说明 V0：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段1-删除项说明-V0.md`
- 删除项说明 V1：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段1-删除项说明-V1.md`
- 删除项说明 V2：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段1-删除项说明-V2.md`
- 删除项说明 V3：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段1-删除项说明-V3.md`
- 删除项说明 V4：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段1-删除项说明-V4.md`
- 删除项说明 V5：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段1-删除项说明-V5.md`
- 删除项说明 V6：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段1-删除项说明-V6.md`
- 删除项说明 V7：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段1-删除项说明-V7.md`
- 阶段1最终交付文档 V1：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段1-最终交付文档-V1.md`
- 阶段2页面结构草图 V1：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段2-页面结构草图-V1.md`
- 阶段2页面结构草图 V2：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段2-页面结构草图-V2.md`
- 阶段2最终交付文档 V0：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段2-最终交付文档-V0.md`
- 阶段2最终交付文档 V1：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段2-最终交付文档-V1.md`
- 阶段2最终交付文档 V2：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段2-最终交付文档-V2.md`
- 阶段3页面原型与 MVP 功能拆解 V0：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段3-页面原型与MVP功能拆解-V0.md`
- 阶段3页面原型与 MVP 功能拆解 V1：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段3-页面原型与MVP功能拆解-V1.md`
- 阶段3页面原型与 MVP 功能拆解 V2：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段3-页面原型与MVP功能拆解-V2.md`
- 阶段3 UI 低保真线框图 V0：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段3-UI低保真线框图-V0.md`
- 阶段3 UI 低保真线框图 V1：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段3-UI低保真线框图-V1.md`
- 阶段3 UI 低保真线框图 V2：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段3-UI低保真线框图-V2.md`
- 阶段3 MVP 开发任务清单 V0：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段3-MVP开发任务清单-V0.md`
- 阶段3 MVP 开发任务清单 V1：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段3-MVP开发任务清单-V1.md`
- 阶段3最终交付文档 V0：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段3-最终交付文档-V0.md`
- 阶段3最终交付文档 V1：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段3-最终交付文档-V1.md`
- 阶段3最终交付文档 V2：`/home/ubuntu/Workspace/math-young-lecturer/docs/阶段3-最终交付文档-V2.md`
- 积分规则展示与成长能量机制 V0：`/home/ubuntu/Workspace/math-young-lecturer/docs/积分规则展示与成长能量机制-V0.md`
- 全站 UI 视觉风格指南 V1：`/home/ubuntu/Workspace/math-young-lecturer/docs/全站UI视觉风格指南-V1.md`
- 全站 UI 视觉风格示例 V0 HTML：`/home/ubuntu/Workspace/math-young-lecturer/docs/全站UI视觉风格示例-V0.html`
- 全站 UI 视觉风格示例 V0 PNG：`/home/ubuntu/Workspace/math-young-lecturer/docs/全站UI视觉风格示例-V0.png`
- 网站知识产权与数据安全保护机制 V0：`/home/ubuntu/Workspace/math-young-lecturer/docs/网站知识产权与数据安全保护机制-V0.md`
- 项目库 AI 协作生成机制 V0：`/home/ubuntu/Workspace/math-young-lecturer/docs/项目库AI协作生成机制-V0.md`
- 全站 UI 视觉风格指南 V0：`/home/ubuntu/Workspace/math-young-lecturer/docs/全站UI视觉风格指南-V0.md`
- 执行流程：`/home/ubuntu/Workspace/math-young-lecturer/docs/【数学小讲师联盟】·执行流程V0.md`
- 设计总纲：`/home/ubuntu/Workspace/math-young-lecturer/docs/数学小讲师联盟·设计总纲-V0.md`

---

## 最近操作记录

- 2026-06-09 10:25 CST：阶段1-F针对犟爸反馈“手机版效果不符合方案 B、首页 Hero 区域内容框被屏幕吞掉”完成本地自检与代码修复。根因：手机版品牌容器/全局兜底仍为 `76px` 且局部缺 `overflow: visible`，站名字距与方案 B 预览不一致；首页手机端 Hero 缺少单列 `minmax(0,1fr)`、标题换行、视觉框宽度约束与问题卡片 `max-width`。本轮修改 `components/navbar.tsx`、`app/globals.css`、`app/page.tsx`，将手机品牌容器收紧为 `70px`、Logo 保持 `57px × 35px`、站名紧凑居中且不裁切；Hero 手机端改为自适应 padding、单列 grid、标题 `clamp(32px,10.4vw,40px)` 并允许 `.hi` 换行，视觉框和问题卡片限制在屏幕内。新增 `tests/stage1f-mobile-navbar-home-hero-fit-rules.test.mjs`，已先确认旧代码红灯；修复后相关规则测试 `24/24` 通过，`npm run build` 通过。注意：当前会话尝试连接生产服务器 `159.75.144.28` 时可用 SSH 身份均返回 `Permission denied (publickey,password)`，因此本条为“本地修复 + 构建通过”，尚未完成公网部署/PM2 reload/线上复验；交付文档：`docs/阶段1-F-手机版导航方案B与首页Hero自适应修复交付-V0.md`。

- 2026-06-09 10:06 CST：阶段1-F按犟爸确认的“方案 B｜轻巧平衡”完成导航 Logo 与登录入口调整并部署。本轮先按要求预览确认，不直接上线；确认后从未登录态导航栏删除独立【登录】按钮，保留 `/login` 页面、受保护入口 307 跳登录页与退出登录 callback，用户仍可从其他分页/受保护入口进入登录流程。`components/navbar.tsx` 将站名加 `brand-title` 稳定类名，桌面 Logo 调整为 `70px × 43px`、手机 Logo `57px × 35px`，品牌区上下居中、`gap:0`；`app/globals.css` 同步更新 Stage1-F 全局兜底为方案 B，并移除旧 `.forest-site-nav .login` 强边框兜底。更新 4 个导航/缓存规则测试；相关规则测试 `26/26` 通过，`npm run build` 通过。已部署到公网并验证：公共导航页 `/`、`/qa`、`/projects`、`/hall`、`/login` 均 200 且 no-store，`/teacher`、`/admin`、`/profile` 未登录 307 到 `/math-young-lecturer/login`，`/api/user/profile` 未登录 401，PM2 online、unstable restarts 0；线上 CSS `999f4102e45290d6.css` 已含方案 B 尺寸且不含旧登录按钮强边框，线上 JS chunk 不含未登录态导航登录按钮渲染逻辑。交付文档：`docs/阶段1-F-导航登录按钮删除与Logo方案B部署交付-V0.md`。

- 2026-06-09 09:38 CST：阶段1-F针对犟爸反馈“没看到修复后的效果”完成线上可见性根因处理并部署。自检发现此前代码虽已上线，但导航样式主要位于 JS/styled-jsx chunk，且 `/qa`、`/projects`、`/hall` 等公共导航页面仍出现 `x-nextjs-cache: HIT`、未统一 no-store，用户端可能复用旧页面壳/旧导航样式，因此看到的仍是旧效果。本轮修复：`middleware.ts` 将 `/`、`/qa`、`/projects`、`/hall`、`/login`、`/profile` 纳入 no-store，并补充首页 matcher；`app/globals.css` 增加 Stage1-F 导航即时可见兜底覆盖，用新 CSS 文件强制 Logo/站名/登录按钮样式：品牌区 `76px`、`gap:0`、站名居中贴在 Logo 正下方，Logo 默认 `22px × 14px`、桌面 `24px × 15px`、手机 `20px × 13px`，`.login` 独立 `border:2px solid #184638` + 浅黄色 halo。新增 `tests/stage1f-navbar-visible-deploy-cache-rules.test.mjs`，相关测试 `17/17` 通过，`npm run build` 通过，已部署并公网验证 `/math-young-lecturer`、`/qa`、`/login`、`/projects`、`/hall` 均返回 no-store，线上 CSS `0444954634cd8736.css` 已包含强制 Logo/登录边框规则，PM2 online。交付文档：`docs/阶段1-F-导航修复未即时可见根因处理交付-V0.md`；代码提交：`e589a7a stage1f: force visible navbar refresh`。

- 2026-06-09 09:27 CST：阶段1-F按犟爸复核反馈“Logo 仍偏大、站名需居中贴在 Logo 正下方、只看到【退出登录】有边框没看到【登录】边框”完成导航 Logo 与登录按钮二次修复并部署。自检确认上一版 Logo 仍可继续收紧，且【登录】与【退出登录】共用弱边框导致未登录态视觉不够明显；本轮将品牌区宽度从约 `84px` 收紧到 `76px`，桌面 Logo 收紧到约 `26px × 16px`、手机 Logo 收紧到约 `22px × 14px`，Logo 与站名 `gap: 0`，站名 `width: 100%`、`text-align: center`、`line-height: 1`，确保“数学小讲师联盟”紧贴并居中位于 Logo 正下方；同时为 `.login` 单独加 `border: 2px solid #184638` 和浅黄色外描边，避免只看到【退出登录】边框。新增/更新规则测试覆盖更小 Logo、紧凑居中站名、登录独立强边框与首页入口，相关测试 `29/29` 通过，`npm run build` 通过，已按 worktree standalone 安全部署并公网验证 `/`、`/qa`、`/login` 200，`/teacher`、`/admin` 未登录 307 到登录页，PM2 `math-young-lecturer` online，线上 JS chunk 已验证包含二次收紧后的 Logo/登录边框规则。交付文档：`docs/阶段1-F-导航Logo与登录按钮二次修复交付-V0.md`；代码提交：`88e6427 stage1f: tighten navbar brand and login border`；文档提交：`568f3fe docs: record stage1f navbar second pass`。

- 2026-06-09 08:54 CST：阶段1-F按犟爸反馈完成导航 Logo、登录按钮与首页入口修复并部署。处理内容：`components/navbar.tsx` 在【你问我答】前新增【首页】；将 Logo 与“数学小讲师联盟”改为紧凑上下堆叠整体，桌面端 Logo 约 `34px × 21px`、手机端约 `28px × 17px`，品牌区居中且锁定宽度，避免溢出导航栏；为【登录】/【退出登录】按钮增加更清晰的 `1.5px` 小边框与轻微高亮外描边，提高可发现性。新增 `tests/stage1f-navbar-logo-login-home-rules.test.mjs` 并更新旧导航规则；相关规则测试 29/29 通过，`npm run build` 通过。提交 `4b1bcb6 stage1f: refine navbar logo login and home link`；已按 worktree standalone 正确结构部署并 reload PM2，线上 `math-young-lecturer` online。公网验证：`/` 跟随跳转后 200，`/qa` 200，`/login` 200，`/teacher` 与 `/admin` 未登录仍 307 到登录页；线上 JS chunk 已验证包含【首页】位于【你问我答】前、桌面/手机紧凑 Logo 尺寸、登录按钮边框与高亮，并且仍未恢复 `workspaceLinks` / `getVisibleWorkspaceNavForRole` 额外角色入口逻辑。交付文档：`docs/阶段1-F-导航Logo登录按钮首页入口修复交付-V0.md`。

- 2026-06-09 08:35 CST：阶段1-F按犟爸反馈“学习者、老师、管理者登录后不应新增【个人中心】、【老师工作台】、【管理员后台】按钮，直接把原有【个人中心】做唯一入口”完成导航唯一身份入口修正并部署。处理原则：公共导航只保留【你问我答】【项目营】【成果广场】和唯一【个人中心】；不再登录后追加 `workspaceLinks`；唯一【个人中心】按角色分流，学习者进入 `/profile`，老师进入 `/teacher`，管理者进入 `/admin`，未登录仍进入 `/profile` 登录/成长护照引导链路。新增 `tests/stage1f-single-personal-entry-nav-rules.test.mjs`，更新角色边界与导航简洁规则；相关规则测试 41/41 通过，`npm run build` 通过。提交 `e535ed1 stage1f: keep personal center as sole role entry`；已按 worktree standalone 正确结构部署并 reload PM2，线上 `math-young-lecturer` online。公网验证：`/` 跟随跳转后 200，`/login` 200，`/teacher` 与 `/admin` 未登录仍 307 到登录页；线上 JS chunk 不再包含 `getVisibleWorkspaceNavForRole` / `workspaceLinks`，导航中保留唯一【个人中心】构造。交付文档：`docs/阶段1-F-个人中心唯一入口导航修正交付-V0.md`。

- 2026-06-09 08:03 CST：阶段1-F按犟爸反馈“导航栏要保持简洁，删减重复功能入口；老师工作台和管理员后台 UI 需更新到最新版本”完成导航精简与后台视觉统一并部署。判断【你问我答】已承担提问业务入口，登录按钮旁再放独立【我要提问】会造成重复，因此删除桌面 `className="ask"` 和移动 `mobile-ask`，保留主导航【你问我答】。老师工作台、管理员后台从旧 `guardian-*` / `sticker-*` 表面类升级为 `forest-workspace-shell`、`forest-workspace-hero`、`forest-dashboard-card`、`forest-panel`、`forest-card`、`forest-empty` 等最新 Morning Paper / Forest Ink 体系。新增 `tests/stage1f-nav-workspace-ui-rules.test.mjs`，更新 `tests/u0c-visual-consistency-rules.test.mjs`；相关规则测试 37/37 通过，`npm run build` 通过。提交 `a556831 stage1f: streamline nav and refresh workbench ui`；已部署并 reload/restart PM2，线上 `/` 200、`/login` 200、`/teacher` 和 `/admin` 未登录 307 到登录页，老师/管理员线上 page chunk 均包含 `forest-workspace-shell` / `forest-workspace-hero` 且不再包含 `guardian-workbench`。部署中曾因漏同步 standalone 根 `node_modules` 出现短暂 502，已补同步 `.next/standalone/node_modules/` 并恢复公网 200。交付文档：`docs/阶段1-F-导航精简与后台视觉统一交付-V0.md`。

- 2026-06-09 07:38 CST：阶段1-F按犟爸反馈“现在可以正常登录，但需要补充账户【退出登录】按钮”完成账户退出入口补齐并部署。判断手机端此前隐藏 `.nav-actions`，仅依赖导航退出不够稳定，因此在 `app/profile/page.tsx` 学生成长护照顶部账户区域新增明确“退出登录”按钮，直接调用 NextAuth `signOut({ callbackUrl: "/math-young-lecturer/login" })`；同时将桌面导航 `components/navbar.tsx` 原“退出”文案改为“退出登录”，退出后同样回到部署路径登录页，便于切换学生/老师账号。新增 `tests/stage1f-account-logout-rules.test.mjs` 并先确认红灯；修复后相关规则测试 27/27 通过，`npm run build` 通过。提交 `1d9679e stage1f: add account logout entry`；已按 worktree standalone 正确结构部署并 reload PM2，线上 `math-young-lecturer` online。公网验证：`/profile` 200 且 no-store，`/login` 200 且 no-store，线上 JS chunk 已包含“退出登录”文案和 `/math-young-lecturer/login` callbackUrl。交付文档：`docs/阶段1-F-账户退出登录按钮补齐交付-V0.md`。

- 2026-06-09 07:23 CST：阶段1-F按犟爸最新反馈“学生账号登录后显示 Application error: a client-side exception has occurred”完成客户端异常根因修复并部署。本轮先核对用户确认的学生账号 `18800010001`：账号存在、角色 `STUDENT`、已开放、已设置密码和姓名，排除账号角色/开放状态/无密码问题；随后继续下钻到个人中心完整渲染链路，定位 `app/profile/page.tsx` 在学生资料加载成功后渲染“成长能量从哪里来 / 成长能量有什么用”时使用 `GROWTH_ENERGY_SOURCES`、`GROWTH_ENERGY_USES`，但此前未从 `growth-passport-ui-rules.mjs` 显式导入，导致浏览器端 ReferenceError，Next.js 显示通用 Application error。新增 `tests/stage1f-profile-client-reference-rules.test.mjs`，先确认红灯；修复导入后，相关规则测试 25/25 通过，`npm run build` 通过。提交 `ff58953 stage1f: fix profile growth passport client references`；已按 worktree standalone 正确结构部署并 reload PM2，线上 `math-young-lecturer` online。公网验证：`/profile` 200 且 no-store，线上 profile chunk `app/profile/page-ae16d6c865555d34.js` 已包含成长能量来源/用途文案，且不再保留未解析的 `GROWTH_ENERGY_SOURCES` / `GROWTH_ENERGY_USES` 引用名。交付文档：`docs/阶段1-F-学生登录后客户端异常修复交付-V0.md`。

- 2026-06-09 07:06 CST：阶段1-F按犟爸最新反馈“手机端用学生账号依然无法登录”完成登录 session 读取竞争修复并部署。本轮重新拆分登录链路，确认线上学生账号 active=15/15、无 password hash 用户为 0，问题重点转向登录成功后前端立即 `getSession()` 的时序风险。原 `app/login/page.tsx` 在 `signIn(... redirect:false)` 成功后立刻读取 session 并用 `session?.user?.role` 决定跳转；手机浏览器/微信内置浏览器可能已写入 cookie 但 session 尚未刷新，导致按未知角色路径处理，表现为学生账号“无法登录”。新增 `tests/stage1f-login-session-race-rules.test.mjs`，修改登录页新增 `waitForPostLoginSession()`：最多短暂重试 5 次、每次 150ms；若读到角色则按角色分流，若登录成功但短时间仍读不到角色，则优先完整加载学生成长护照 `/math-young-lecturer/profile`。提交 `cd22739 stage1f: wait for student login session`；规则测试 24/24 通过，`npm run build` 通过；已部署并 reload PM2，线上 `math-young-lecturer` online。公网 iPhone/微信 UA 复验：`/login` 200 且 no-store，新登录 chunk 包含重试等待逻辑；临时 STUDENT 账号复验：精确激活 updated=1，登录接口 200，session role=STUDENT，`/api/user/profile` 200，profile 页面 200，临时账号已删除。交付文档：`docs/阶段1-F-学生账号手机端登录失败修复交付-V0.md`。

- 2026-06-09 06:55 CST：阶段1-F按犟爸再次反馈“还是不正常”完成登录/个人中心旧缓存根因修复并部署。本轮重新检查公网响应头，确认 `/login` 与 `/profile` 曾返回 `cache-control: s-maxage=31536000, stale-while-revalidate`，身份敏感入口被静态长缓存，手机浏览器/微信内置浏览器可能持续拿旧 HTML 与旧 JS chunk，导致上一轮已部署的 `window.location.assign('/math-young-lecturer/profile')` 仍不生效。新增 `tests/stage1f-auth-entry-cache-rules.test.mjs`，修改 `middleware.ts`：将 `/login`、`/profile` 纳入 no-store 响应头覆盖，设置 `Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0`、`Pragma: no-cache`、`Expires: 0`；更新 `stage0h` 规则，允许 `/profile` 进入 middleware 做缓存头覆盖但不改成强制登录跳转。提交 `98e9cdc stage1f: prevent stale auth entry shell cache`；规则测试 22/22 通过，`npm run build` 通过；已部署并 reload PM2，线上 `math-young-lecturer` online。公网 iPhone/微信 UA 复验：`/login` 200 且 no-store，`/profile` 200 且 no-store，`/projects` 仍包含 `href="/math-young-lecturer/profile"`；临时 STUDENT 登录态复验 session 200、`/api/user/profile` 200、profile 页面 200。重要副作用：本轮临时账号激活脚本参数传递失误，曾将现有用户 active 状态统一置为 true；临时账号已删除，当前线上用户统计 total=18、active=18、inactive=0，需后续按运营名单人工复核。交付文档：`docs/阶段1-F-登录个人中心旧缓存修复交付-V0.md`。

- 2026-06-09 06:35 CST：阶段1-F按犟爸最新反馈完成“登录后个人中心仍出错”修复并部署。犟爸反馈手机端点击【个人中心】会进入登录页，但填写账号登录后依旧进入错误页面；本轮确认根因在 `app/login/page.tsx` 登录成功后仍通过 `router.push(getPostLoginRedirectPath(...))` 跳相对 `/profile`，学生账号会重新进入手机端客户端路由/RSC 预取/旧 chunk 链路。已新增 `tests/stage1f-login-redirect-rules.test.mjs`，修改 `app/login/page.tsx`：学生登录成功后如果目标为 `/profile`，改用 `window.location.assign(PUBLIC_PROFILE_HREF)` 完整加载 `/math-young-lecturer/profile`；老师/管理员保持角色工作台逻辑。提交 `7d5903b stage1f: harden student login redirect`；规则测试 20/20 通过，`npm run build` 通过；已按 worktree standalone 正确结构部署并 reload PM2，线上 `math-young-lecturer` online。手机 UA + 临时 STUDENT 登录态复验：登录目标 `/math-young-lecturer/profile`，session 200，`/api/user/profile` 200，profile 页面 200；线上登录 chunk 已含 `window.location.assign(c.pS)`，公共 chunk 中 `c.pS` 构造为 `/math-young-lecturer/profile`。交付文档：`docs/阶段1-F-登录后个人中心跳转修复交付-V0.md`。

- 2026-06-09 06:24 CST：阶段1-F按犟爸手机端复测反馈完成个人中心入口二次修复并部署。犟爸反馈手机登录后导航栏【个人中心】与项目营【查看我的成长护照】仍进入出错页；本轮判断上一轮主要修复 `/profile` 页面内部容错，但手机端仍可能受客户端路由/RSC 预取/旧 chunk 缓存链路影响，因此将这两个关键入口改为部署 basePath 下的完整文档跳转 `/math-young-lecturer/profile`。新增 `lib/public-entry-hrefs.mjs`、`tests/stage1f-mobile-profile-entry-rules.test.mjs`，修改 `components/navbar.tsx`、`app/projects/page.tsx` 与原 profile 入口规则测试。提交 `b08fe70 stage1f: harden mobile profile entry navigation`；规则测试 19/19 通过，`npm run build` 通过。部署时发现 standalone `server.js` 位于 `.next/standalone/.worktrees/batch9-review-authorization/server.js`，已按正确结构补发到 `/var/www/math-young-lecturer` 并 reload PM2；线上 `math-young-lecturer` 状态 online。手机 UA + 临时 STUDENT 登录态复验：登录 302 跳 `/math-young-lecturer/profile`，session 200，`/api/user/profile` 200，`/projects` 与 `/profile` 均 200，页面 HTML 均包含 `href="/math-young-lecturer/profile"` 且不再出现 `href="/profile"` 的相对个人中心入口；临时账号已清理。交付文档：`docs/阶段1-F-手机端个人中心入口二次修复交付-V0.md`。

- 2026-06-09 06:06 CST：阶段1-F按犟爸线上反馈完成个人中心入口故障修复并部署。用户反馈路径：导航栏【个人中心】与项目营【查看我的成长护照】均进入出错页面；本次修复 `app/profile/page.tsx`，新增 NextAuth `status` 等待态、个人资料 API 失败的页面内诚实降级、历史讲题/项目记录缺关联时的容错文案，并新增 `tests/stage1f-profile-entry-resilience-rules.test.mjs`。提交 `d6328e0 stage1f: harden profile entry fallback`；规则测试 17/17 通过，`npm run build` 通过；已 rsync 部署并重启 PM2 `math-young-lecturer`，状态 online、unstable restarts 0。公网验证：`/projects` 200 且成长护照按钮 href 为 `/math-young-lecturer/profile`，`/profile` 200，未登录 `/api/user/profile` 401，临时 STUDENT 账号登录后 `/api/user/profile` 200 且返回 user；临时验证账号已清理。交付文档：`docs/阶段1-F-个人中心入口故障修复交付-V0.md`。

- 2026-06-09 05:30 CST：阶段1-F第一轮入口真实性审计完成，新增 `docs/阶段1-F-入口真实性第一轮审计清单-V0.md`。线上验证：`/login`、`/qa`、`/qa/ask`、`/projects`、`/hall`、`/profile` 均 200，`/teacher`、`/admin` 未登录 307 跳 `/math-young-lecturer/login`，未复现重复 basePath；`/api/user/profile`、`/api/teacher/outcomes`、`/api/admin/workbench` 未登录均 401。规则测试 `stage1-p0-closure`、`stage0h-profile-mobile-nav`、`role-access` 共 6/6 通过。审计发现下一批 P0/P0.5：示例项目 `projectType=PAID` 但 `price=0` 冲突；成果广场 `outcomes=[]` 有空壳感；线上历史答案存在 `status=APPROVED` 但 `reviewStatus=PENDING`；老师工作台与登录注册仍用 `alert()` 做核心反馈。

- 2026-06-09 05:22 CST：按犟爸指令进入阶段1-F。阶段1-F定位为“入口真实性与业务闭环推进”，优先做全站入口清单可视化、未登录/无权限真实反馈、试运营空状态、后台可追踪状态与假入口降级；已新增推进单 `docs/阶段1-F-入口真实性与业务闭环推进单-V0.md`。本阶段不是继续视觉修饰，验收口径为：主要按钮必须有真实动作、状态反馈或明确降级说明，阶段结束必须生成最终交付文档。

- 2026-06-08 23:32 CST：阶段1｜入口真实性审计与 P0 真实业务闭环补齐第一轮完成并部署。代码提交 `3b84748 stage1: close p0 authenticity gaps`。本轮不是继续视觉优化，而是针对“按钮/入口是否真实运行”做 P0 补齐：讲解审核同步 `status/reviewStatus`，老师待审核讲解队列排除空占位，小组协作空间 GET/POST 增加成员/项目访问权/老师管理员权限检查，咨询二维码上传限制为管理员。新增 `tests/stage1-p0-closure-rules.test.mjs`，完整验证链与 `npm run build` 通过；线上 `/`、`/qa`、`/projects`、`/hall`、`/profile`、`/login` 与核心 API 验证通过，PM2 `math-young-lecturer` online，unstable restarts 为 0。交付文档：`docs/阶段1-入口真实性审计与P0闭环补齐最终交付文档-V1.md`。边界：仍不能称为全站所有按钮/业务闭环都已完成，下一步应继续阶段1后续批次做入口清单可视化、报名开通、上传作品、假入口降级。

- 已进入阶段1。
- 已生成首页内容清单 V0。
- 犟爸已确认首页讨论方向，并补充“免费注册即可提问、自主认领、系统自动锁定、发布前审核”的加入机制。
- 已生成首页内容清单 V1。
- 已生成分页内容清单 V0。
- 犟爸确认：成果广场作为独立分页；问答闭环和 PBL 项目营同为 P0；个人中心与家长进度页合并为成长中心；主导航应收敛为少量主要入口；老师登录后自动进入审核工作台。
- 已生成分页内容清单 V1。
- 犟爸提出需区分未登录/已登录可见边界，并建议：问答广场更名为“你问我答”；成长中心改为“个人中心”；登录/注册不作为主导航页，而是在需要登录的操作中弹窗触发；成果广场、项目营、你问我答可公开浏览但操作需登录。
- 已生成分页内容清单 V2。
- 已生成删除项说明 V0。
- 犟爸确认删除项关键问题：登录/注册不作为主导航；老师工作台不向家庭账号展示；个人中心作为统一入口并按身份分流；成果广场只展示已审核授权内容；项目任务书/协作空间需登录/参与/解锁；免费基础项目与收费进阶项目分层；不公开排行榜和积分，但公开徽章/身份标签。
- 已生成删除项说明 V1。
- 阶段1三项交付物已完成，建议进入阶段1最终整理。
- 犟爸进一步确认：不再使用“家庭账号”作为前台身份，账号身份改为学习者账号、老师账号、管理员账号；学习者账号就是孩子账号，家长只辅助操作；前台身份不要过多，简化为提问官、小讲师、项目官，并采用身份并列点亮、星级成长、成长护照徽章墙的结构。
- 已生成删除项说明 V2。
- 犟爸继续提出：提问官、小讲师、项目官三个主身份命名中“官”字重复，可优化为提问者/小讲师/项目探索者；能力徽章放入成长护照且不公开展示；能力徽章解锁机制应明确，不宜只靠积分，而应采用老师标签累计触发。
- 已生成删除项说明 V3。
- 犟爸继续确认：主身份标签应更短，采用提问者/小讲师/探索家；主身份展示位置需要明确，建议头像旁轻展示、点击头像弹出孩子身份卡片、个人中心成长护照完整展示；主身份星级与能力徽章解锁应分开，主身份星级采用有效行为数量 + 老师审核通过 + 积分辅助，能力徽章采用老师标签累计触发。
- 已生成删除项说明 V4。
- 犟爸继续提出：老师提交反馈和评语需要区分免费体验与付费项目的服务边界；免费体验应尽量自动化反馈，老师主要负责审核和能力标签；免费项目可发自动结营证书；付费项目可以提供个性化反馈和更完整的结营证书；小讲师视频反馈也以自动化为准，老师仅手动贴能力标签，不做默认具体点评。
- 已生成删除项说明 V5。
- 犟爸继续追问：提问不应作为付费项目，也不适合老师做问题改进服务；小讲师视频不应按服务包提供讲法建议，孩子有自己的讲法，只要讲清楚即可；需要重新明确付费到底发生在哪个环节，以及如何在不做基础会员制的情况下支付老师审核成本。
- 已生成删除项说明 V6：建议 P0 付费核心放在进阶项目包/主题项目营；免费社区保留完整闭环但设置温和提交限额；老师审核成本通过自动化、限额和项目包收入反哺。
- 犟爸继续追问：免费提问额度应按每个孩子计算，每周 1–3 个问题；免费基础项目可开放 1–3 个，成熟后到 1–5 个；免费项目若需要线上破冰会占用老师时间，需明确协作模式；异步协作容易冷场，应设计系统节奏与退出标记；累计 3 次无故退出后暂停新的体验项目机会。
- 已生成删除项说明 V7：明确免费额度对象、基础项目数量、免费项目异步轻协作模式、付费项目同步节点，以及项目退出记录/冷却机制。
- 犟爸确认删除说明部分没有问题，可以继续推进。
- 已生成阶段1最终交付文档 V1，整合首页内容清单 V1、分页内容清单 V2、删除项说明 V7，并将当前阶段推进到阶段2页面结构草图准备。
- 犟爸确认首页首屏按钮采用“我要提问 / 看看孩子怎么学”的方向：前者进入提交问题流程，后者跳转首页内部“孩子怎么学”流程模块；“我要讲题”放入你问我答场景中，表达为“我要当小讲师”或“去认领一道题”。
- 已生成阶段2页面结构草图 V1，覆盖 5 个主页面和关键弹窗/子页面，当前待犟爸确认后进入页面线框图/原型 V0。
- 犟爸补充并确认阶段2新增产品机制：项目知识点标签、认领贴士、站内协作留痕、48小时上传规则、公开分享授权、成长护照助人/合作数据、头像、首页按钮、我要当小讲师、后台权限、老师工作台、温暖提示语、公开数据、分享长图。
- 已生成阶段2页面结构草图 V2。
- 已生成阶段3页面原型与 MVP 功能拆解 V0。
- 犟爸确认每个阶段结束都要生成最终交付文档，且最终交付文档必须以最新结论为准，吸收保留内容，排除已被推翻、替换、降级内容，不机械拼接历史版本。
- 已对照《项目流程控制台》完成阶段2验收，并生成阶段2最终交付文档 V0。
- 犟爸对阶段2最终交付文档提出6项补充疑问：分享授权降低门槛、项目解锁条件和自动组队、免费/付费权益对比、付费报名联系方式、教师项目权限、项目后儿童反馈问卷。
- 已吸收上述6项补充并生成阶段2最终交付文档 V1。
- 犟爸继续补充11项阶段2最终规则：首屏副标题与年级标签、能力观察标签命名、审核通过通知、题目拍照上传/图片识别/热度、项目有效时长与永久回看、免费项目质量边界、证书能力展示与成果提交形式、请假退出与付费退出规则、成果点赞与行动号召、主身份灰色/点亮与一句话介绍、邀请分享影响力统计。
- 已吸收上述11项补充并生成阶段2最终交付文档 V2。
- 犟爸确认阶段2没有问题，要求将阶段3原型文档同步升级到阶段2 V2规则。
- 已生成阶段3页面原型与 MVP 功能拆解 V1，补齐首页定位、问题拍照/热度、分享范围、项目解锁/组队/有效期、请假退出、证书、点赞、身份点亮、邀请影响力、教师权限等原型与功能拆解。
- 犟爸对阶段3 V1提出12项反馈：低年级提问表单、72小时认领、分享范围、项目反馈频率、我的影响力、响应式导航、能力星云、教育性文案、热度说明、付费项目包、退费规则、统一封面、付费/退费记录。
- 已生成阶段3页面原型与 MVP 功能拆解 V2，作为后续 UI 低保真线框图和 MVP 开发任务拆解依据。
- 犟爸确认可以进入 UI 低保真线框图 V0。
- 已生成阶段3 UI 低保真线框图 V0，覆盖前台主要页面、核心弹窗、老师工作台、管理员后台、响应式规则与 MVP 页面优先级。
- 犟爸对阶段3 UI低保真线框图提出10项反馈：首页卖点标签、适合/暂不适合人群客观条件、你问我答推荐讲解标题、问题标题说明、小讲师认领弹窗视频建议、项目所需人数与自动解锁、付费报名弹窗与企业微信二维码、协作空间消息提醒、视频/项目成果封面差异与落地方式、提问阶段分享海报。
- 已生成阶段3 UI 低保真线框图 V1，吸收上述10项反馈。
- 犟爸确认V1客观检查意见：首页标签改“真实思考”；适合谁/暂不优先适合谁各精简为3条；其他关于移动端标题、项目开组、积分表达、付费报名、封面落地、影响力去增长化等建议全部采纳。
- 已生成阶段3 UI 低保真线框图 V2，作为进入MVP开发任务清单前的稳定低保真稿。
- 已生成阶段3 MVP 开发任务清单 V0，覆盖开发总原则、当前代码基础、数据库模型、前端页面、后端API、权限、通知、自动开组、审核、测试验收、开发批次与P1后置清单。
- 犟爸对MVP开发任务清单V0提出8项反馈：身份星级数据规则、审核清晰度标签逻辑、成果广场作品转发、老师项目编辑删除与封面上传、三类角色页面严格区分、1–3年级知识点标签、公开老师企业微信入口、积分流水运转规则。
- 已生成阶段3 MVP 开发任务清单 V1，吸收并客观修正上述反馈。
- 已完成全站数据动线自检，确认提问者、小讲师、项目探索、付费咨询、成果广场、个人中心、老师/管理员后台、积分流水、身份星级、通知与权限等核心链路均有清晰运转规则。
- 已生成阶段3最终交付文档 V0，整合阶段3页面原型 V2、UI低保真线框图 V2、MVP开发任务清单 V1，并排除旧版中已被替换/降级的内容。
- 犟爸补充提出数据资产与视觉统一问题：问题库、项目库、小讲师视频数据库应明确呈现在哪些工作台，下载/导出权限应限定为管理员；AI生成项目应进入项目草稿库，经人工审核后由管理员发布；同时需要单独设计全站UI视觉风格指南。
- 已生成阶段3最终交付文档 V1，补充数据资产工作台归属、管理员导出/下载权限、AI生成项目入库与发布流程；已生成全站UI视觉风格指南 V0。
- 犟爸继续补充3项问题：网站知识产权保护/反扒机制；UI视觉指南需要具体图片示例；项目库如何与AI协作、是人工导入还是网站一键生成，以及AI生成项目的底层逻辑和产出水准。
- 已生成阶段3最终交付文档 V2、网站知识产权与数据安全保护机制 V0、项目库AI协作生成机制 V0、全站UI视觉风格指南 V1，并制作全站UI视觉风格示例 HTML 与 PNG 截图。


## 相关文档路径

### 阶段4准备｜代码实施前工程实施计划 V0
- `/home/ubuntu/Workspace/math-young-lecturer/docs/代码实施前工程实施计划-V0.md`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/代码实施管控与回退预案-V0.md`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/superpowers/plans/2026-06-01-math-young-lecturer-mvp.md`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/阶段4-工程基线检查报告-V0.md`

### 阶段3补充｜三类分享海报高保真模板 V1
- `/home/ubuntu/Workspace/math-young-lecturer/docs/三类分享海报高保真模板-V1-说明.md`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/三类分享海报高保真模板-V1.html`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/三类分享海报高保真模板-V1.png`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/分享海报-问题发芽-V1.png`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/分享海报-讲解长高-V1.png`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/分享海报-项目成林-V1.png`

### 阶段3补充｜三类分享海报高保真模板 V0
- `/home/ubuntu/Workspace/math-young-lecturer/docs/三类分享海报高保真模板-V0-说明.md`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/三类分享海报高保真模板-V0.html`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/三类分享海报高保真模板-V0.png`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/分享海报-V0-我提出了一个好问题.png`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/分享海报-V0-我把这个方法讲清楚了.png`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/分享海报-V0-我们一起完成了一个项目.png`


- `/home/ubuntu/Workspace/math-young-lecturer/docs/首页高保真视觉稿-V1-说明.md`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/首页高保真视觉稿-V1-灵动生命力版.html`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/首页高保真视觉稿-V1-灵动生命力版.png`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/首页高保真视觉稿-V1-灵动生命力版-mobile.png`


- `/home/ubuntu/Workspace/math-young-lecturer/docs/首页高保真视觉稿-V0-说明.md`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/首页高保真视觉稿-V0-无限生长版.html`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/首页高保真视觉稿-V0-无限生长版.png`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/首页高保真视觉稿-V0-无限生长版-mobile.png`


- `/home/ubuntu/Workspace/math-young-lecturer/docs/全站UI视觉风格指南-V2-无限生长版.md`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/全站UI视觉风格示例-V1-无限生长版.html`
- `/home/ubuntu/Workspace/math-young-lecturer/docs/全站UI视觉风格示例-V1-无限生长版.png`


### 2026-05-29｜UI视觉风格 V2「无限生长版」

- 犟爸反馈：V1 风格中规中矩、同质化，希望首页第一眼有惊喜感，并融入 Logo 中“两个孩子握手形成数学 ∞”以及“问题小树苗 → 讲题强壮小树 → 项目森林”的成长隐喻。
- 处理结论：采纳，并将 UI 方向从“温暖纸面感”升级为“无限生长的数学森林”。
- 已新增：`全站UI视觉风格指南-V2-无限生长版.md`、`全站UI视觉风格示例-V1-无限生长版.html`、`全站UI视觉风格示例-V1-无限生长版.png`。
- 设计原则：视觉惊喜不靠炫技，而靠产品理念本身可视化；∞ 作为隐形秩序，提问/讲题/项目作为成长三节点，避免游戏化排行与廉价卡通。


### 2026-05-29｜首页高保真视觉稿 V0

- 用户确认：认可“无限生长版”方向，可以进入首页高保真视觉稿 V0。
- CTA 处理：采纳“看孩子如何成长”，替换原“看看孩子怎么学”；理由是更呼应小树苗成长隐喻，同时仍作为理解型 CTA 跳转首页成长路径模块。
- 已生成：`首页高保真视觉稿-V0-无限生长版.html`、桌面 PNG、移动端 PNG、`首页高保真视觉稿-V0-说明.md`。
- 自检修正：压缩首屏过长留白，增大首屏右侧成长节点字号，调整“问题发芽 / 讲解长高 / 项目成林”节点位置。


### 2026-05-29｜首页高保真视觉稿 V1（灵动生命力版）

- 用户反馈：V0 文字偏多、信息拥挤；希望适当精简说明文字，改用动图/图片动线呈现；整体更灵动、有生命力，更符合低龄段孩子活泼朝气；增加温暖、走心、有人味儿的小设计。
- 处理结论：采纳。V1 保留主标题“让一个好问题，长成一片数学森林”，确认 CTA“我要提问 / 看孩子如何成长”。
- 已生成：`首页高保真视觉稿-V1-灵动生命力版.html`、桌面 PNG、移动端 PNG、`首页高保真视觉稿-V1-说明.md`。
- 视觉策略：减少长段文案，采用“一个问题正在长大”的 ∞ 动线、场景图卡、身份成长卡和温暖小纸条，如“谢谢你把问题说出来”“慢慢讲，我们听得见”。

- 2026-05-29 16:12 CST：犟爸认可首页高保真视觉稿 V1 后，生成三类分享海报高保真模板 V0，覆盖“我提出了一个好问题 / 我把这个方法讲清楚了 / 我们一起完成了一个项目”，并完成整体预览、三张 PNG 导出与核验。
- 2026-06-01：犟爸反馈三类分享海报 V0 文字密集、视频海报小树遮挡文字、三张区分度不够；已升级生成三类分享海报 V1，并将“图案不得遮挡文字安全区”固化为后续海报/封面硬规则。
- 2026-06-01：犟爸认可三类分享海报 V1，确认可继续往下推进；已生成《代码实施前工程实施计划 V0》与 agentic workers 实施计划；已完成批次0工程基线检查，`npm run build` 通过；已补充《代码实施管控与回退预案 V0》，下一步待犟爸确认 8 个开工问题后进入批次1产品常量与视觉 token 抽离。
- 2026-06-02：犟爸确认积分规则展示建议全部采纳；已新增《积分规则展示与成长能量机制 V0》。归档结论：积分/成长能量的完整说明放在个人中心/成长护照；提问、认领讲题、项目报名、作品提交等行动页做即时轻提示；首页不突出积分；家长视角补充“成长能量不用于公开排名、不代表谁更聪明”的说明；项目卡使用准备度/参与条件表达，不公开具体积分门槛。
- 2026-06-08：按犟爸确认进入阶段0，采用此前最终讨论确定的 UI 设计方案（全站 UI 视觉风格指南 V2｜无限生长版 + 首页高保真视觉稿 V1｜灵动生命力版），完成首轮线上内测部署。该首轮部署修复了 basePath 登录跳转并加入内部测试提示，但后续复盘确认：首轮只是旧首页局部改文案和提示，不能认定为最终高保真 UI 已落地。阶段0 V0 交付文档保留为过程记录，但其“UI 已完成”表述降级处理。
- 2026-06-08：阶段0补救完成。重新读取 `全站UI视觉风格指南-V2-无限生长版.md`、`首页高保真视觉稿-V1-说明.md`、`首页高保真视觉稿-V1-灵动生命力版.html`，以 V1 高保真 HTML 为视觉母版重构 `app/page.tsx` 首页，不再在旧首页上局部修补。线上地址 `http://159.75.144.28/math-young-lecturer` 已部署提交 `ad85f75`，首页包含“数学森林 / 问题发芽 / 讲解长高 / 项目成林 / 温暖小纸条 / 森林任务”等 V1 关键元素；`npm run build`、专项规则测试、线上路径/API 验证通过。补救交付文档：`/home/ubuntu/Workspace/math-young-lecturer/.worktrees/batch9-review-authorization/docs/阶段0-新UI内测部署补救交付文档-V1.md`。
- 2026-06-02 10:26 CST：完成代码实施前自检并补齐缺口：将 docs 规划与交付文档纳入 Git 提交 `c09d9b1`，补充 `.gitignore` 忽略 `tsconfig.tsbuildinfo` 与 `.worktrees/`，创建基线标签 `baseline-before-mvp`，建立隔离 worktree `batch1/product-tokens`，在批次1中新增 `lib/product-copy.ts`、`lib/visual-tokens.ts`、`components/brand/infinity-logo.tsx`、`components/brand/growth-motifs.tsx`、`components/ui/badge.tsx`、`components/ui/card.tsx`，并接入首页/导航/布局；`npm run build` 通过。
- 2026-06-02 10:39 CST：继续推进批次1产品常量与视觉 token 抽离：将首页信任背书、行动按钮、流程提示等核心文案抽到 `lib/product-copy.ts`；将涂鸦符号、身份色、动效/旋转参数等抽到 `lib/visual-tokens.ts`；移除首页旧植物 SVG 重复定义，导航栏移除重复移动端菜单按钮；`npm run build` 通过，旧文案与重复定义扫描为 0。
- 2026-06-02 10:52 CST：完成批次1收尾：新增 `components/home/flow-arrow.tsx` 与 `components/home/trust-card.tsx`，将首页流程箭头、信任背书卡片组件化；将首页阶段路径、阶段卡片、展开说明、底部数学涂鸦整理为清晰子组件；补充 `verticalWavyLineDataUrl`、`dashedPanelBorder`、`tornPaperRadius`、`bottomMathDoodles` 等视觉 token；`npm run build` 通过，首页体积约 `6.03 kB`。
- 2026-06-02 11:08 CST：进入批次2成长能量 / 积分账本数据模型与接口准备。已按 TDD 新增 `tests/growth-energy-rules.test.mjs` 并确认 RED；新增 `lib/growth-energy-rules.mjs`，沉淀成长能量原因、儿童提示文案、私密展示和“不直接兑换身份星级”规则；扩展 `prisma/schema.prisma` 与 `prisma/seed-data.sql`，补齐 `PointTransaction` 的 `displayLabel`、`userMessage`、`sourceType`、`visibility`、`affectsIdentityLevel`，新增 `IdentityProgress`、`AbilityTagLog`、`QuestionHeat`、`ShareAsset`、`ExportLog` 等模型；讲解审核/采纳接口改为写入私密成长能量流水，个人中心接口返回成长能量摘要、最近流水和身份进度；`npm run test:growth-energy`、`DATABASE_URL=... npx prisma validate`、`npm run build` 均已通过。
- 2026-06-02 11:30 CST：进入批次3你问我答 P0 数据字段与流程接口落地。已按 TDD 新增 `tests/qa-flow-rules.test.mjs` 并确认 RED；新增 `lib/qa-flow-rules.mjs`，沉淀提问字段归一、72小时认领窗口、热度唯一/只排序、问题审核通过生成私密提问成长能量等规则；接入 `app/api/questions`、`app/api/questions/[id]`、`app/api/questions/[id]/claim`、`app/api/questions/[id]/heat`、`app/api/teacher/questions/[id]/review` 与 `app/api/answers`，落实未审核内容不公开、同一学习者同题只加一次热度、认领超时释放、分享范围待审核后生效；`npm run test:qa-flow`、`npm run test:growth-energy`、`DATABASE_URL=... npx prisma validate`、`DATABASE_URL=... npx prisma generate`、`npm run build` 均已通过。
- 2026-06-02 11:47 CST：进入批次4你问我答前端页面接入 / UI 流程落地。已按 TDD 新增 `tests/qa-ui-rules.test.mjs` 并确认 RED；新增 `lib/qa-ui-rules.mjs`，沉淀提问表单步骤、分享范围、状态徽章、热度反排名说明、72小时认领提示、讲解提交审核提示与老师审核文案；接入 `app/qa/page.tsx`、`app/qa/ask/page.tsx`、`app/qa/question/[id]/page.tsx` 与 `app/teacher/page.tsx`，落实拍照/图片链接、建议标题、困惑类型、分享范围、热度按钮、认领倒计时、讲解提交分享范围、老师问题审核入口；`npm run test:qa-ui`、`npm run test:qa-flow`、`npm run test:growth-energy`、`DATABASE_URL=... npx prisma validate`、`DATABASE_URL=... npx prisma generate`、`npm run build` 均已通过。
- 2026-06-02 12:16 CST：进入批次5个人中心 / 成长护照 / 我的数学成长能量 UI 落地。已按 TDD 新增 `tests/growth-passport-ui-rules.test.mjs` 并确认 RED；新增 `lib/growth-passport-ui-rules.mjs`，沉淀成长能量来源、用途、反排名说明、三身份星级卡、最近流水温暖文案；重写 `app/profile/page.tsx`，将旧“积分/徽章墙”个人中心升级为成长护照，展示当前成长能量、来源/用途、孩子/家长/老师说明、提问者/小讲师/探索家 1–3 星、最近成长能量记录、能力徽章、我的提问/讲题/项目；`npm run test:growth-passport-ui` 与 `npm run build` 已通过，后续将继续跑完整回归验证。
- 2026-06-02 12:53 CST：进入批次6项目营 / 项目卡准备度与参与条件 UI 落地。已按 TDD 新增 `tests/project-camp-ui-rules.test.mjs` 并确认 RED；新增 `lib/project-camp-ui-rules.mjs`，沉淀项目类型、准备度/参与条件、组队状态、有效期、报名意向与探索成长提示规则；重写 `app/projects/page.tsx` 与 `app/projects/[id]/page.tsx`，项目卡不展示具体积分门槛，改为展示准备度、知识标签、组队状态、有效期、免费/主题服务边界和老师人工确认提示；`npm run test:project-camp-ui` 与 `npm run build` 已通过，后续将继续跑完整回归验证。
- 2026-06-02 13:15 CST：进入批次7项目协作空间 / 任务卡 / 作品提交 UI 落地。已按 TDD 新增 `tests/project-collab-ui-rules.test.mjs` 并确认 RED；新增 `lib/project-collab-ui-rules.mjs`，沉淀项目协作空间、任务卡、协作记录类型、作品提交、项目反馈、探索成长提示与 AI 使用边界规则；重写 `app/groups/[id]/page.tsx`，接入小组任务卡、今日探索重点、留言流/过程记录、讨论想法、过程记录、作品提交、项目反馈入口；增强 `app/api/groups/[id]/messages/route.ts` 返回小组/项目元信息，并把作品/反馈作为带类型标签的协作记录提交；`npm run test:project-collab-ui` 与 `npm run build` 已通过，后续将继续跑完整回归验证。
- 2026-06-02 13:35 CST：进入批次8成果广场 / 分享授权 / 统一作品展示 UI 落地。已按 TDD 新增 `tests/outcome-hall-ui-rules.test.mjs` 并确认 RED；新增 `lib/outcome-hall-ui-rules.mjs`，沉淀成果广场文案、成果类型、审核+授权公开过滤、统一成果卡、成果详情结构与分享边界规则；重写 `app/api/hall/route.ts`，只返回老师审核通过且 `shareScope=PUBLIC_HALL` 的公开成果；新增 `app/api/hall/[id]/route.ts` 与 `app/hall/[id]/page.tsx`；重写 `app/hall/page.tsx`，接入问题发芽/讲解长高/项目成林筛选、统一成果卡、详情入口、分享授权说明和反排名说明；`npm run test:outcome-hall-ui` 与 `npm run build` 已通过，后续将继续跑完整回归验证。
- 2026-06-02 14:36 CST：完成部署包规范化收尾。已按 Next.js standalone + PM2 部署规则重新生成规范包 `/tmp/math-young-lecturer-e397e2b-normalized.tar.gz`，包内根层同时包含 `server.js`、`node_modules/next`、`.next/static`、`public`、`prisma` 与 `DEPLOY_BUILD.json`，避免 worktree 打包只带 inner app root 导致 `Cannot find module 'next'`。服务器 release `/var/www/math-young-lecturer.normalized-e397e2b` 已通过临时 PM2 冒烟测试，正式切换前已备份当前部署到 `/var/backups/math-young-lecturer/math-young-lecturer-before-normalized-20260602-143524.tar.gz`；正式目录 `/var/www/math-young-lecturer` 当前 build metadata 为 `e397e2b`、`package_mode=normalized-standalone-root-plus-worktree-app`。公网验证首页、你问我答、项目营、成果广场、个人中心均 200 且包含批次1–8关键文案；公开 API `/api/hall`、`/api/projects`、`/api/questions?status=OPEN` 均 200；PM2 清空日志后重新访问未产生新错误。
- 2026-06-02 14:56 CST：进入并完成批次9老师工作台审核闭环增强 / 成果授权管理 / 分享撤回入口。已按 TDD 新增 `tests/review-authorization-rules.test.mjs` 并确认 RED→GREEN；新增 `lib/review-authorization-rules.mjs`，沉淀成果广场准入、授权撤回、项目作品草稿、老师成果审核 tab 与授权状态文案规则；扩展 `prisma/schema.prisma`，新增 `ProjectArtifact` 正式项目作品表，补齐 `ShareAsset` 的 `updatedAt`、`teacherNote`、`authorizedAt`、`withdrawnAt`、`withdrawalReason` 与项目作品关联，`ContentReviewStatus`/`ShareScope` 增加 `WITHDRAWN`；增强 `app/api/groups/[id]/messages/route.ts`，协作空间提交“作品提交”时自动沉淀为待审项目作品；新增 `app/api/teacher/outcomes`、`app/api/teacher/outcomes/review`、`app/api/outcomes/withdraw`，支持成果审核、公开授权与撤回；老师工作台 `app/teacher/page.tsx` 新增“成果审核 / 授权管理 / 已撤回”闭环入口；成果广场列表与详情 API 接入项目作品，公开展示必须满足老师审核通过 + `PUBLIC_HALL`，撤回后保留学习记录但不再公开；同时修正项目营公开文案，避免显示内部条件词。验证已通过：全量规则回归、`DATABASE_URL=... npx prisma validate`、`npm run build`、公开页面/组件/lib 文案敏感词扫描。

---

## 2026-06-02 15:39 CST｜批次9线上部署完成

- 部署目标：`http://159.75.144.28/math-young-lecturer`
- 线上版本：`94e9650 feat: add outcome review authorization workflow`
- 部署方式：沿用规范化 Next.js standalone + PM2 + nginx 路径代理流程。
- 本地验证：`npm run test:review-authorization` 5/5 通过，`npm run test:project-camp-ui` 5/5 通过，`npm run build` 通过。
- 部署包：`/tmp/math-young-lecturer-94e9650-normalized.tar.gz`
- 部署包 sha256：`0041c205aa56d723db85b7dabcd17f514f7c4811b7925daabe0aeb317e035e44`
- 线上备份：已在 `/var/backups/math-young-lecturer/` 生成批次9切换前代码备份、数据库备份和 manifest。
- 数据库同步：已基于批次9 schema 完成 `project_artifacts` 新表、`share_assets` 新字段、`WITHDRAWN` 状态等同步；未执行清库或 force reset。
- Prisma 运行时修复：standalone 裁剪包不含 Prisma generator，远端 release 中已按项目版本 `@prisma/client@5.22.0` / `prisma@5.22.0` 重新生成客户端，解决临时冒烟中 Prisma 运行时版本不匹配问题。
- 临时 PM2 冒烟：`math-young-lecturer-batch9-smoke` 已验证 `/profile`、`/teacher`、`/hall`、`/api/hall` 均 200，`/api/teacher/outcomes` 未登录返回 401，符合权限边界；日志清空后无新错误。
- 正式切换：`/var/www/math-young-lecturer` 已切换至批次9 release，PM2 `math-young-lecturer` online，unstable restarts 为 0。
- 公网验证：`/`、`/profile`、`/teacher`、`/hall`、`/qa`、`/projects`、`/api/hall`、`/api/projects`、`/api/questions?status=OPEN` 均返回 200；`/api/teacher/outcomes` 未登录返回 401；公网静态 chunk 已命中“成果审核 / 授权管理 / 已撤回 / 公开授权已撤回 / 项目作品”等批次9文案。
- PM2 日志复查：正式进程日志清空后重触发关键页面和 API，无新的 stdout/stderr 错误。

---

## 2026-06-02 16:27 CST｜登录后按身份跳转热修复

- 用户反馈：老师账号实际已登录，但登录后直接跳转首页，体验上像“无法进入老师工作台”。
- 根因：`app/login/page.tsx` 登录成功后固定 `router.push("/")`，未按 `session.user.role` 跳转。
- 修复：新增 `lib/login-redirect-rules.ts` / `.mjs` 与 `tests/login-redirect-rules.test.mjs`；登录后通过 `getSession()` 读取角色，`TEACHER` / `ADMIN` 跳 `/teacher`，`STUDENT` 跳 `/profile`，未知角色回首页。
- 验证：`node --test tests/login-redirect-rules.test.mjs` 3/3 通过，`npm run build` 通过。
- 线上版本：`6ae0aa1 fix: redirect users by role after login`。
- 部署：按规范 standalone 包热修复部署，当前线上 `DEPLOY_BUILD.json` commit 为 `6ae0aa1`，PM2 online，unstable restarts 为 0。
- 公网验证：`/login`、`/teacher`、`/profile`、`/api/auth/csrf` 均 200；老师测试账号登录后 session role 为 `TEACHER`；PM2 日志清空后重触发页面无新错误。

---

## 2026-06-03 07:30 CST｜159 服务器部署通道恢复与 basePath 热修复部署

- 部署目标：`http://159.75.144.28/math-young-lecturer`
- SSH 通道：已确认可使用 `/home/ubuntu/.hermes/keys/leohermes.pem` 登录 `ubuntu@159.75.144.28`；之前误用其它 key 会出现 `Permission denied (publickey,password)`。
- 线上版本：`3421596 fix: avoid duplicate base path redirects`，分支 `batch9/review-authorization` 已推送到 GitHub。
- 根因修正：部署前临时 PM2 冒烟发现 `/math-young-lecturer/teacher` 被旧的裸路径重定向规则处理成 `/math-young-lecturer/math-young-lecturer/teacher`；已补充 `requestBasePath` 判断，避免 Next.js basePath 下重复追加 basePath。
- 本地验证：`node --test tests/legacy-path-redirect-rules.test.mjs tests/login-redirect-rules.test.mjs tests/review-authorization-rules.test.mjs` 12/12 通过；`npm run build` 通过（项目仍按现有配置跳过 TypeScript 类型校验）。
- 部署包：`/tmp/math-young-lecturer-3421596-normalized.tar.gz`
- 部署包 sha256：`fbeea213fed315d05a6f6d660d460b705f775092801313c2ee08bc9f51933fbb`
- 线上备份：`/var/backups/math-young-lecturer/math-young-lecturer-before-3421596-20260603-072321.tar.gz`
- 临时 PM2 冒烟：`/math-young-lecturer/login`、`/math-young-lecturer/teacher`、`/math-young-lecturer/api/hall` 通过；`/math-young-lecturer/teacher` 返回 200，未再出现重复 basePath 跳转。
- 正式切换：`/var/www/math-young-lecturer` 已切换至 3421596 release，PM2 `math-young-lecturer` online，unstable restarts 为 0。
- 公网验证：`/`、`/login`、`/teacher`、`/profile`、`/qa`、`/projects`、`/hall`、`/api/hall`、`/api/projects`、`/api/questions?status=OPEN` 均返回 200；老师测试账号登录后 session role 为 `TEACHER`；页面中未检出 `math-young-lecturer/math-young-lecturer` 重复路径。
- PM2 日志复查：正式进程日志清空后重触发关键页面和 API，无新的 stdout/stderr 错误。


---

## 2026-06-08 19:35 CST｜阶段0-B 全站分页 UI 视觉统一补救完成

- 触发原因：首页高保真补救和全站导航统一后，前台其他分页主体仍保留旧 UI 结构，不能称为全站 UI 完成。
- 补救范围：`/qa`、`/qa/ask`、`/projects`、`/hall`、`/profile` 五个前台主要分页主体。
- 视觉实现：新增 `forest-page-shell`、`forest-page-hero`、`forest-panel`、`forest-card`、`forest-empty`、`forest-mission-card` 等阶段0-B统一森林视觉壳层；分页文案分别收敛为问题种子、森林任务、森林展墙、成长护照、不排名等已确认方向。
- 测试约束：新增 `tests/stage0b-forest-ui-rules.test.mjs`，先确认 RED，再实现 GREEN，用测试防止“只改导航、不改分页主体”的问题复发。
- 本地验证：阶段0-B测试 3/3 通过；成长能量、QA流程、QA UI、成长护照、项目营、项目协作、成果广场、审核授权、路径/登录/权限相关规则测试均通过；`npm run build` 通过。
- 线上版本：`c7b4048 stage0b: unify front pages with forest UI`。
- 部署包：`/tmp/math-young-lecturer-c7b4048-stage0b-forest-ui-20260608193126-normalized.tar.gz`。
- 部署包 sha256：`bd5161398598179fe74422270bc286029da77ee1fd4be7048f51c65ef0730293`。
- 公网验证：`/qa`、`/qa/ask`、`/projects`、`/hall` 均 200 且包含森林壳层与新导航；`/profile` 未登录 307 跳转登录，符合权限边界；`/api/hall`、`/api/projects`、`/api/questions?status=OPEN` 均 200。
- 边界说明：登录页、老师工作台、管理员后台、项目详情、问题详情、协作空间、成果详情仍未完成全站森林视觉统一；业务真实闭环仍需后续入口真实性审计和 P0 闭环补齐。
- 交付文档：`docs/阶段0-B-全站分页UI视觉统一补救交付文档-V1.md`。


---

## 2026-06-08 20:30 CST｜U0-C 全站视觉一致性收口完成

### 本次状态

U0-C 已完成并部署上线，线上提交：`fcd86b0 stage0c: tighten visual consistency across deep routes`。

本轮不是继续做大规模高保真重设计，而是做全站视觉一致性收口：

- 登录页 `/login` 纳入森林壳层与统一导航；
- 问题详情 `/qa/question/[id]` 纳入森林详情壳层；
- 项目详情 `/projects/[id]` 纳入森林详情壳层；
- 项目协作空间 `/groups/[id]` 纳入森林详情壳层；
- 成果详情 `/hall/[id]` 纳入森林详情壳层与空状态；
- 老师工作台 `/teacher` 改为克制的“守林人工作台｜老师”壳层；
- 管理员工作台 `/admin` 改为克制的“守林人工作台｜管理员”壳层。

### 验证

- 新增 `tests/u0c-visual-consistency-rules.test.mjs`，3/3 通过；
- 阶段0-B 前台森林 UI 规则测试 3/3 通过；
- QA、成长护照、项目营、协作空间、成果广场、审核授权、登录路径等既有测试全部通过；
- `npm run build` 通过；
- 线上 PM2 `math-young-lecturer` online，unstable restarts 为 0；
- 线上 `/login`、`/qa`、`/qa/ask`、`/projects`、`/hall`、示例项目详情、示例问题详情均可访问；
- `/profile`、`/teacher`、`/admin` 未登录正常跳转 `/math-young-lecturer/login`；
- `/api/hall`、`/api/projects`、`/api/questions?status=OPEN` 正常，受保护老师接口 401 正常。

### 边界

U0-C 完成后，不再建议继续卡在 UI 大改。当前可以进入阶段1：全站入口真实性审计 + P0 真实业务闭环补齐。

视觉层仍需人工走查截图确认，但不应阻塞阶段1。

详细交付文档：`docs/U0-C-全站视觉一致性收口交付文档-V1.md`

### 2026-06-08 20:50 CST｜阶段0-D V2 分页视觉严格对齐完成

- 背景：犟爸反馈分页风格仍然奇怪，尤其小图标、信息框与首页高保真 V1 气质不一致。
- 判断：反馈成立。阶段0-B/U0-C主要解决统一壳层与割裂，但分页内部仍保留旧 sticker、裸 emoji、厚重手绘边框，未严格贴合《全站 UI 视觉风格指南 V2》。
- 执行：新增 Stage0-D V2 分页视觉层，补齐 V2 精确色彩 token、CSS 小图标、信息框、温暖纸条、视觉卡与封面 glyph。
- 范围：`/qa`、`/qa/ask`、`/projects`、`/hall`、`/profile`。
- 测试：新增 `tests/stage0d-v2-visual-fidelity-rules.test.mjs`；Stage0-D / Stage0-B / U0-C 规则测试均通过，`npm run build` 通过。
- 部署：线上提交 `8055112 stage0d: align front pages with V2 visual system` 已部署。
- 线上验证：`/qa`、`/qa/ask`、`/projects`、`/hall` 均 200，均含 `forest-v2-icon` 与 V1/V2 信息卡，无重复 basePath；API `/api/hall`、`/api/projects`、`/api/questions?status=OPEN` 均 200。
- 记录：详见 `docs/阶段0-D-V2分页视觉严格对齐交付文档-V1.md`。
- 边界：本轮是前台分页视觉气质严格对齐，不是业务闭环补齐；下一步仍建议进入阶段1入口真实性审计。

### 2026-06-08 21:35 CST｜阶段0-E 分页背景残留与按钮填充修正完成

- 背景：犟爸确认阶段0-D分页视觉进步明显，同时指出进入下一阶段前需重点检查分页背景旧代码残留、按钮框背景填充不完整。
- 判断：反馈成立。`app/globals.css` 中仍有 Stage0-B 旧分页背景定义与旧 `.hand-btn::before` 手绘内边框逻辑，虽然后置样式已覆盖大部分视觉，但仍有回退/不一致风险。
- 修正：清理旧 `.forest-page-shell` 米色纸面渐变和固定数学符号背景；新增 Stage0-E V2 权威分页背景；禁用 `.hand-btn::before` 旧内边框；强制 `hand-btn` 背景填满整个胶囊按钮框，并覆盖 `bg-crayon-*` opacity variant。
- 测试：新增 `tests/stage0e-background-button-cleanup-rules.test.mjs`；Stage0-E / Stage0-D / Stage0-B / U0-C 规则测试均通过，`npm run build` 通过。
- 部署：线上提交 `1efb821 stage0e: clean paging backgrounds and button fills` 已部署。
- 线上验证：`/qa`、`/qa/ask`、`/projects`、`/hall`、`/login` 均 200；旧背景残留 false、旧固定符号背景 false、无重复 basePath；线上 CSS 包含按钮填充修正规则。
- 记录：详见 `docs/阶段0-E-分页背景残留与按钮填充修正交付文档-V1.md`。
- 判断：当前 UI 阶段可进入阶段1，后续视觉仅做局部小修，不再阻塞真实业务闭环。

### 2026-06-08 21:58 CST｜阶段0-F RootLayout 旧背景清理与手机导航收口完成

- 背景：犟爸指出分页背景依然有旧版本残留，手机版首页导航栏太拥挤，分页标题之间可用“｜”分隔。
- 根因：上一轮只处理 `.forest-page-shell` 与按钮，但全局 `app/layout.tsx` 仍在 `<body>` 挂载 `paper-grid` 与多个 `math-doodle-*` 旧背景层；`app/page.tsx` 首页也曾使用 global body/body:before/body:after 背景，存在分页观感污染风险。
- 修正：移除 RootLayout 旧 `paper-grid` 与 `math-doodle-*` 挂载；在 `globals.css` 防御性禁用旧背景类；首页背景改为 `.forest-home` 局部作用域；手机导航新增 `mobile-quick-links`，以“你问我答｜项目营｜成果广场｜个人中心”紧凑显示分页标题。
- 测试：新增 `tests/stage0f-background-nav-cleanup-rules.test.mjs`；Stage0-F / Stage0-E / Stage0-D / Stage0-B / U0-C 规则测试均通过，`npm run build` 通过。
- 部署：线上提交 `de0c1b2 stage0f: remove legacy backgrounds and compact mobile nav` 已部署。
- 线上验证：`/`、`/qa`、`/qa/ask`、`/projects`、`/hall`、`/login` 均 200；HTML 中 `paper-grid false`、`math-doodle false`、`mobile-quick-links true`、`｜` 分隔 true、无重复 basePath；API 正常。
- 记录：详见 `docs/阶段0-F-RootLayout旧背景清理与手机导航收口交付文档-V1.md`。
- 判断：阶段0视觉收口可进入阶段1；如用户手机实测仍看到残留，下一步必须要求截图/具体页面位置来定位，不再凭 CSS 覆盖猜测。

### 2026-06-08 22:17 CST｜阶段0-G 手机版导航文字重叠修正完成

- 背景：犟爸指出网站导航栏依然不正常，手机版导航栏文字重叠，并要求 Logo 图标缩小，“数学小讲师联盟”放在 Logo 正下方。
- 根因：阶段0-F手机导航仍是单行 flex，Logo、分页标题、菜单按钮共用一行，小屏宽度不足时会挤压重叠；同时手机端品牌文字被隐藏，未满足 Logo 下方显示的要求。
- 修正：手机端 `@media (max-width: 620px)` 下将导航改为两行 grid；第一行居中显示小 Logo + 下方“数学小讲师联盟”；第二行单独显示“你问我答｜项目营｜成果广场｜个人中心”；菜单按钮固定右上角；分页标题行支持横向滚动防重叠。
- 测试：扩展 `tests/stage0f-background-nav-cleanup-rules.test.mjs`，新增手机导航两行结构、Logo 缩小、品牌文字下置、分页标题独占整行与 `overflow-x:auto` 规则；Stage0-F / Stage0-E / Stage0-D / Stage0-B / U0-C 规则测试均通过，`npm run build` 通过。
- 部署：线上提交 `ff72042 stage0g: fix compact mobile navbar layout` 已部署。
- 线上验证：`/`、`/qa`、`/projects`、`/hall`、`/login` 均 200；API `/api/hall`、`/api/projects`、`/api/questions?status=OPEN` 均 200；PM2 `math-young-lecturer` online，unstable restarts 为 0。
- 记录：详见 `docs/阶段0-G-手机版导航重叠修正交付文档-V1.md`。
- 判断：本轮只修手机版导航，不改业务逻辑；手机端确认后建议进入阶段1入口真实性审计与 P0 真实业务闭环补齐。

### 2026-06-08 22:38 CST｜阶段0-H 个人中心与手机版导航修正完成

- 背景：犟爸指出【个人中心】页面无法正常打开；手机版导航栏需要去掉分页折叠按钮；Logo 需要再缩小，导航文字靠右对齐。
- 根因：`/profile` 被 middleware 页面级拦截，未登录时直接跳 `/login`，导致 `app/profile/page.tsx` 里已有的登录引导壳层无法渲染；手机端仍保留菜单按钮，且 Logo/分页标题未按右对齐要求处理。
- 修正：从 middleware matcher 移除 `/profile` 与 `/profile/:path*`，让个人中心页面壳层可直接打开；`/api/user/profile` 仍由 `/api/:path*` 保护；profile API 增加 user 不存在时的 404 防护；手机端隐藏 `.nav-actions`、`.menu-button`、`.mobile-menu`；Logo 缩小为 `24px × 16px`；品牌与分页标题右对齐。
- 测试：新增 `tests/stage0h-profile-mobile-nav-rules.test.mjs`；Stage0-H / Stage0-F / Stage0-E / Stage0-D / Stage0-B / U0-C / role-access 规则测试均通过，`npm run build` 通过。
- 部署：线上提交 `bc668a3 stage0h: fix profile shell and phone navbar` 已部署。
- 线上验证：`/profile` 200 且不再跳 `/login`；`/`、`/qa`、`/projects`、`/hall`、`/login` 均 200；`/api/user/profile` 未登录 401 符合保护要求；API `/api/hall`、`/api/projects`、`/api/questions?status=OPEN` 均 200；PM2 online，unstable restarts 为 0。
- 记录：详见 `docs/阶段0-H-个人中心与手机版导航修正交付文档-V1.md`。

### 2026-06-08 22:50 CST｜阶段0-I 手机版 Logo 实际尺寸锁定修正完成

- 背景：犟爸指出 Logo 图标尺寸不对，手机版看起来反而更大。
- 根因：阶段0-H 只设置了手机端 SVG `width/height: 24px/16px`，没有同步锁定 `max-width/max-height/flex/display`，移动端 WebView / flex 环境中仍可能出现视觉偏大或被布局撑开的感受。
- 修正：手机端 Logo 改为 `18px × 12px`，并增加 `max-width:18px`、`max-height:12px`、`flex:0 0 18px`、`display:block`，锁定实际渲染外框。
- 测试：更新 `tests/stage0h-profile-mobile-nav-rules.test.mjs` 与 `tests/stage0f-background-nav-cleanup-rules.test.mjs`；两项规则测试通过，`npm run build` 通过。
- 部署：线上提交 `290bab6 stage0i: lock down mobile logo size` 已部署。
- 线上验证：`/`、`/profile`、`/qa` 均 200；线上构建产物已确认包含 `width:18px;height:12px`、`max-width:18px;max-height:12px`、`flex:0 0 18px`、`display:block`；PM2 online，unstable restarts 为 0。
- 记录：详见 `docs/阶段0-I-手机版Logo尺寸锁定修正交付文档-V1.md`。

### 2026-06-08 23:08 CST｜阶段0-J 手机版 Logo 未生效根因修正完成

- 背景：犟爸指出手机版 Logo 还是没有改过来。
- 根因：阶段0-I 只修改了 `.forest-brand svg` 尺寸，但 `LogoMark()` 是子组件；styled-jsx 编译后选择器变为 `.forest-brand.jsx-xxxx svg.jsx-xxxx`，子组件内 SVG 没有对应 `jsx-xxxx` class，导致真实 DOM 上规则不能稳定命中。
- 修正：给 SVG 显式增加 `className="brand-logo-mark"`，CSS 改为 `.forest-brand :global(.brand-logo-mark)`；手机端尺寸进一步锁定为 `14px × 9px`，并增加 `max-width/max-height/flex/display` 防拉伸。
- 测试：更新 Stage0-H 与 Stage0-F 导航规则测试，要求显式 Logo class 与 global selector；测试通过，`npm run build` 通过。
- 部署：线上提交 `deccb49 stage0j: target mobile logo svg globally` 已部署。
- 线上验证：`/`、`/profile`、`/qa` 均 200；HTML 包含 `brand-logo-mark`；构建产物包含 `.forest-brand.jsx-... .brand-logo-mark{width:14px;height:9px;max-width:14px;max-height:9px;...}`；PM2 online，unstable restarts 为 0。
- 记录：详见 `docs/阶段0-J-手机版Logo未生效根因修正交付文档-V1.md`。
