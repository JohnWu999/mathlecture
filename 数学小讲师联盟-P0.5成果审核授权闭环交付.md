# 数学小讲师联盟 P0.5 成果审核授权闭环交付文档

交付时间：2026-06-03
阶段：P0.5 第三块 —— 成果广场真实内容生产与审核授权闭环
线上地址：http://159.75.144.28/math-young-lecturer

---

## 一、本阶段目标

本阶段解决“成果广场不能只是空页面或展示壳”的问题，把项目作品/讲题成果从真实提交、老师审核、公开授权、成果广场展示、撤回下架串成闭环。

核心产品规则：

```text
进入成果广场 = 老师审核通过 + 授权公开
```

也就是：

```text
reviewStatus = APPROVED
shareScope = PUBLIC_HALL
```

只审核通过但未授权公开，不进入成果广场；只申请公开但老师未审核，也不进入成果广场。撤回公开授权后，不删除孩子学习记录，只从成果广场下架。

---

## 二、阶段清单

| 阶段项 | 状态 | 说明 |
|---|---:|---|
| 建立成果审核/授权规则测试 | ✅ | 新增 P0.5 成果审核授权规则测试 |
| 补齐 ProjectArtifact 生命周期规则 | ✅ | 项目作品从协作消息沉淀为待审成果 |
| 接入老师成果队列 API | ✅ | 老师可查看待审核、已公开、已撤回成果 |
| 接入审核/公开/撤回 API | ✅ | 审核、授权、撤回均是真数据库动作 |
| 成果广场公开列表/详情 | ✅ | 只返回审核通过且授权公开的成果 |
| ShareAsset 授权元数据同步 | ✅ | 公开/撤回同步记录 authorizedAt / withdrawnAt |
| 本地测试、Prisma、构建 | ✅ | 77/77 规则测试通过，Prisma valid，Next build 通过 |
| 部署上线 | ✅ | PM2 math-young-lecturer online |
| 线上端到端验证 | ✅ | 学生提交 → 老师公开 → 广场可见 → 撤回下架，全部通过 |

---

## 三、已交付功能

### 1. 项目作品提交生成待审成果

协作空间中的作品提交使用格式：

```text
[作品提交]
孩子/小组的作品说明
作品/过程链接：/math-young-lecturer/uploads/project-artifact/...
```

提交后系统会创建：

```text
ProjectArtifact(reviewStatus=PENDING, shareScope=GROUP_ONLY)
```

这意味着：

- 作品是真实数据库记录；
- 初始状态不公开；
- 等待老师审核；
- 保留 sourceMessage 关系，能追溯到协作记录。

---

### 2. 老师工作台新增成果审核/授权闭环

老师工作台入口：

```text
http://159.75.144.28/math-young-lecturer/teacher
```

成果区域包含三类队列：

1. 成果审核
   - 待审核讲题成果；
   - 待审核项目作品。

2. 授权管理
   - 已审核并公开的成果；
   - 老师可撤回公开授权。

3. 已撤回
   - 展示已撤回公开授权的成果；
   - 明确说明：作品仍保留为学习记录，不在成果广场展示。

老师可执行：

- 通过并公开；
- 通过但仅保留记录；
- 退回补充；
- 撤回公开授权。

---

### 3. 成果广场只展示双因子通过内容

成果广场入口：

```text
http://159.75.144.28/math-young-lecturer/hall
```

公开 API：

```text
GET /math-young-lecturer/api/hall
GET /math-young-lecturer/api/hall/[id]
```

展示条件：

```text
reviewStatus = APPROVED
shareScope = PUBLIC_HALL
```

接口和前端均保留防线：

- API 查询层只查公开授权成果；
- 规则层再次过滤；
- 详情页也只允许公开成果访问；
- 撤回后成果详情返回不可公开查看。

---

### 4. ShareAsset 授权与撤回元数据同步

本次补强了 `ShareAsset` 生命周期同步。

审核通过并公开时，同步：

```text
reviewStatus = APPROVED
shareScope = PUBLIC_HALL
authorizedAt = 当前时间
```

撤回公开授权时，同步：

```text
reviewStatus = WITHDRAWN
shareScope = WITHDRAWN
withdrawnAt = 当前时间
withdrawalReason = 撤回原因
```

这让后续海报、成果封面、外部分享资产都能遵守同一套授权边界。

---

## 四、本次代码变更

### 新增

```text
tests/p05-outcome-review-authorization-rules.test.mjs
```

覆盖规则：

- 成果广场必须审核 + 授权双因子；
- 项目作品提交生成 PENDING / GROUP_ONLY 草稿；
- 审核输入归一化与非法参数拦截；
- 撤回公开授权不删除源记录；
- ShareAsset 公开/撤回元数据同步；
- 老师工作台三类队列；
- 公开文案不得出现排行、积分门槛、比较刺激。

### 修改

```text
lib/review-authorization-rules.mjs
app/api/teacher/outcomes/review/route.ts
app/api/outcomes/withdraw/route.ts
```

主要补强：

- `normalizeOutcomeReviewInput()`；
- `buildWithdrawalUpdate()`；
- `buildShareAssetAuthorizationUpdate()`；
- `sanitizePublicOutcomeCopy()`；
- 审核 API 同步 ShareAsset；
- 撤回 API 同步 ShareAsset；
- 撤回不删除 Answer / Message / ProjectArtifact。

---

## 五、本地验证结果

已执行：

```bash
node --test tests/p05-outcome-review-authorization-rules.test.mjs
node --test tests/*.test.mjs
DATABASE_URL='[REDACTED]' npx prisma validate
DATABASE_URL='[REDACTED]' npx prisma generate
npm run build
```

结果：

```text
规则测试：77/77 通过
Prisma schema：valid
Prisma generate：通过
Next.js build：compiled successfully
```

---

## 六、线上部署结果

线上应用：

```text
PM2 app: math-young-lecturer
状态：online
```

公开接口验证：

```text
GET /math-young-lecturer/api/hall -> HTTP 200
```

PM2 最近日志：

```text
Next.js Ready
未见新增运行错误
```

---

## 七、线上端到端验证结果

使用临时 STUDENT / TEACHER 账号完成验证；账号、密码、凭据均已清理并脱敏为 `[REDACTED]`。

验证链路：

```text
学生登录
→ 在项目协作空间提交 [作品提交]
→ 系统生成 ProjectArtifact(PENDING, GROUP_ONLY)
→ 老师登录
→ 老师成果审核队列看到该作品
→ 老师通过并公开
→ ShareAsset 同步 APPROVED / PUBLIC_HALL / authorizedAt
→ 成果广场列表出现该成果
→ 成果详情 HTTP 200
→ 老师撤回公开授权
→ 成果广场不再展示
→ ProjectArtifact 和 sourceMessage 仍保留
→ ShareAsset 同步 WITHDRAWN / withdrawnAt
→ 清理临时测试数据
```

线上验证结果：

```json
{
  "student_login_role": "STUDENT",
  "teacher_login_role": "TEACHER",
  "submission_created_artifact": true,
  "pending_contains_artifact": true,
  "review_status_after_approve": "APPROVED",
  "share_asset_public_after_review": true,
  "hall_contains_after_approve": true,
  "detail_status_after_approve": 200,
  "hall_hidden_after_withdraw": true,
  "artifact_record_retained_after_withdraw": true,
  "artifact_status_after_withdraw": "WITHDRAWN",
  "artifact_scope_after_withdraw": "WITHDRAWN",
  "share_asset_withdrawn": true
}
```

临时数据清理复查：

```json
{
  "temp_users": 0,
  "temp_projects": 0,
  "temp_artifacts": 0
}
```

---

## 八、当前产品闭环状态

目前已形成以下真实链路：

```text
你问我答：真实提问/上传/认领/讲题/审核
项目营：真实报名意向/企业微信咨询/管理员跟进
协作空间：真实项目记录/附件上传/作品提交
老师工作台：真实审核问题/讲题/项目成果/授权撤回
成果广场：只展示审核通过 + 授权公开的真实成果
管理员后台：报名、权益、数据资产、咨询入口、审计与运营数据
```

这一步之后，成果广场不再是空壳；它已经和孩子真实作品、老师审核、公开授权、撤回机制连上。

---

## 九、仍建议下一步推进

### 优先建议：P0.5 第四块 —— 项目权益开通与报名确认联动

原因：报名现在已经是 PENDING 意向，管理员可以跟进确认，但“确认后开通项目权益”还可以进一步自动/半自动串起来。

建议下一步补：

1. 管理员将报名跟进状态改为 CONFIRMED 后，可一键开通项目权益；
2. UserProjectAccess 与 ProjectRegistration 建立更清晰关联；
3. 学习者个人中心展示“已开通项目权益”；
4. 项目页根据权益显示进入项目/协作空间入口；
5. 管理员后台可筛选“已确认但未开通权益”的待办。

### 备选推进

- 上传迁移对象存储/CDN；
- 管理员报名筛选、导出、批量跟进；
- 成果封面/分享海报真实生成；
- 项目作品后台素材库与安全下载权限。
