# 数学小讲师联盟 P0.5 项目权益开通与报名确认联动交付

## 1. 本次交付目标

本阶段补齐“报名意向 → 人工确认 → 项目权益开通 → 学习者可见/可进入项目”的运营闭环。

核心原则：

1. 报名仍然是意向，不自动成交。
2. 管理员确认适合后，才可以开通项目权益。
3. 项目权益是独立业务记录，不再混用 `user.isActive`。
4. 老师工作台不处理商业权益；老师继续负责教学和审核。
5. 学习者个人中心可以看到自己已开通的项目权益和项目入口。
6. 重复保存确认状态时，不重复创建同一个学生同一个项目的有效权益。

## 2. 已完成内容

### 2.1 规则测试

新增：

- `tests/p05-project-access-linkage-rules.test.mjs`
- `lib/project-access-linkage-rules.mjs`
- `lib/project-access-linkage-rules.ts`

规则覆盖：

1. 只有 `status = CONFIRMED` 且 `followUpStatus = CONFIRMED` 的报名，才可建议/创建项目权益。
2. 非确认报名不得开通项目权益。
3. 指定项目权益必须有学习者与项目。
4. 管理员待办文案强调“确认后开通项目权益”，不使用付款优先语言。
5. 已存在有效项目权益时，不重复开通。
6. 学习者端权益文案是只读展示与项目入口导向。
7. 审计 payload 记录 registration/access/user/project/packageType 关联，不包含联系方式等私密信息。

### 2.2 管理员报名跟进 API 联动开通权益

改造：

- `app/api/admin/registrations/[id]/route.ts`

新增能力：

1. PATCH 报名跟进时支持 `openProjectAccess: true`。
2. 当跟进状态改为 `CONFIRMED` 时，可在同一事务中创建 `UserProjectAccess`：
   - `packageType = SPECIFIC_PROJECT`
   - `status = ACTIVE`
   - `projectId = 报名项目`
   - `userId = 报名学习者`
3. 若该学习者对该项目已存在 `ACTIVE` 权益，则返回成功但不重复创建。
4. 写入两类审计：
   - `UPDATE_REGISTRATION_FOLLOW_UP`
   - `OPEN_PROJECT_ACCESS_FROM_CONFIRMED_REGISTRATION`
5. 未确认报名如果强行 `openProjectAccess`，规则层会拒绝，防止越权/误开通。

### 2.3 管理员后台 UI

改造：

- `app/admin/page.tsx`
- `app/api/admin/workbench/route.ts`

新增能力：

1. 报名意向库每条记录显示权益待办：
   - 报名仍在人工跟进中
   - 报名已确认，待开通项目权益
   - 报名已确认，权益已开通
2. 管理员将跟进状态设为“已确认适合”时，可勾选：
   - “确认后同步开通项目权益”
3. 按钮文案会从“保存跟进状态”变为：
   - “确认跟进并开通权益”
4. workbench 返回报名意向时，会按 `userId + projectId` 挂回已有有效权益，用于 UI 判断是否已开通。

### 2.4 学习者个人中心

改造：

- `app/api/user/profile/route.ts`
- `app/profile/page.tsx`

新增能力：

1. 学习者个人中心项目权益返回项目 `id` 和标题。
2. “我的项目权限”使用儿童友好文案：
   - 已开通
   - 可以进入项目，留下探索过程和作品记录
3. 对 `ACTIVE + SPECIFIC_PROJECT` 权益显示“进入项目 →”链接。
4. 明确保留：权益记录只读，开通仍由管理员后台登记，老师不处理付费/商业权益。

## 3. 本地验证

已执行：

```bash
node --test tests/*.test.mjs
DATABASE_URL='[REDACTED]' npx prisma validate
DATABASE_URL='[REDACTED]' npx prisma generate
npm run build
```

结果：

- 规则测试：84/84 通过
- Prisma schema：valid
- Prisma Client：generate 成功
- Next.js build：Compiled successfully

## 4. 线上部署

已部署到线上：

- 应用：`math-young-lecturer`
- basePath：`/math-young-lecturer`
- PM2 状态：online

部署中继续保留：

1. `/math-young-lecturer` basePath。
2. uploads 共享目录和 nginx alias。
3. 三身份权限边界。
4. PM2 环境变量安全读取方式，不输出数据库连接串或密钥。

## 5. 线上端到端验证

线上真实验证链路：

1. 创建临时管理员、临时学生、临时项目、临时报名意向。
2. 管理员通过真实接口：
   - `PATCH /math-young-lecturer/api/admin/registrations/[id]`
   - payload 包含 `followUpStatus: CONFIRMED` 与 `openProjectAccess: true`
3. 验证报名状态同步为：
   - `status = CONFIRMED`
   - `followUpStatus = CONFIRMED`
4. 验证创建 1 条有效项目权益。
5. 再次重复 PATCH，验证不会重复创建权益。
6. 学生登录后访问：
   - `GET /math-young-lecturer/api/user/profile`
   - 能看到对应项目权益。
7. 管理员 workbench 能看到报名意向已挂回项目权益。
8. 审计日志写入成功。
9. 临时测试账号、项目、报名、权益均已清理。

线上验证结果：

```json
{
  "patch_status_ok": true,
  "registration_confirmed": true,
  "access_created_once": true,
  "duplicate_patch_ok": true,
  "duplicate_prevented": true,
  "profile_status_ok": true,
  "profile_has_project_access": true,
  "workbench_status_ok": true,
  "workbench_registration_has_access": true,
  "audit_written": true
}
```

清理复查：

```json
{
  "temp_users": 0,
  "temp_projects": 0,
  "temp_registrations": 0,
  "temp_accesses": 0
}
```

## 6. 当前产品状态

现在报名运营闭环已经变为：

1. 学生/家长提交报名意向。
2. 企业微信/咨询入口用于人工确认适合度与节奏。
3. 管理员在报名意向库更新跟进状态。
4. 管理员确认适合后，可同步开通该项目权益。
5. 学习者个人中心显示项目权益，并可进入项目。
6. 管理员后台可追踪报名、权益与审计记录。

这一步把“报名确认”和“权益开通”从两个断点连接成了真实运营闭环。